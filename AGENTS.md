# 전공정리프로젝트 — 에이전트 공통 지침 (AGENTS.md)

이 리포에서 작업하는 AI 에이전트용 작업 규칙. Claude Code는 `CLAUDE.md`가 이 문서를 자동으로 읽는다.
작성 2026-07-16 · 개편 2026-07-21(독자 기준 3분할) · **Codex 분업 비활성화 2026-07-27(Claude 단독 운영)**.

---

- 이 문서 = **작업 중 항상 유효한 불변조건.** 유효한 것만 둔다.
- **Codex 분업은 꺼져 있다** — 지금은 Claude 가 콘텐츠·삽화·구현을 모두 한다. 재분업 시 되돌릴 곳은
  `docs/archive/codex-workflow.archived.md` · `docs/codex-setup.md` · `docs/handoff-prompts.md`.
- 분리 문서는 **링크만** 한다. `@import` 하지 않는다.

**★ 문서 배치 원칙 — 「올릴 자격」** (신설 2026-08-14 · 소급 적용 2026-08-16)

- **파일을 열기 *전에* 알아야 하는 것만 이 문서에 둔다.** 열고 나서 필요한 것은 `docs/**` 나 검사
  독스트링에 둔다 — 거기 있다고 잊히지 않는다(검사는 돌고, 문서는 필요할 때 읽힌다).
- **근거는 토큰이 아니라 주의력이다.** 이 문서는 매 세션 통째로 실리고, 너무 길면 절반이 무시된다.
  실측: 2026-08-14 하루에 닫힌 결정을 두 번 다시 열었다 — 여기에도 공용 문서에도 적혀 있었는데도.
- 조항을 추가할 때 자문한다: *"이걸 파일을 열기 전에 몰라도 되나?"* — 몰라도 되면 안 넣는다.
- 표는 `docs/context-budget-plan.md` 의 「내려도 되는 것 / 반드시 남길 것」.

**★★ 크기 — 재는 자가 정본이고, 「삭감」과 「이관」 둘 다 열려 있다**

- **크기를 여기 손으로 적지 않는다** — `python tools/audit_guide_size.py .` 가 정본이다(래칫).
  문서 안에 박은 수는 반드시 낡는다(*"실측 146KB"* 라고 적힌 채 214KB 가 되도록 아무도 안 쟀다).
- **삭감**(2026-08-16 사용자 판정 — 실제 작업에 필요한 최소치만 남긴다): 단위는 «지우기»가 아니라
  **«옮기고 실을 남기기»** 다 — **포인터(원장 날짜 · 도구 이름) 없이 지우지 않는다.** 남기는 것은 셋:
  **규칙 · 판정선 · 전제.** 가는 곳도 셋: 사용자 지적 → 나루 `기록/feedback-ledger.md`
  (조회 `tools/feedback_lookup.py`) · 실측 → 그 도구의 독스트링 · 공용 규칙에 정본이 있는 것 → 포인터 한 줄.
- **이관(`docs/` 로 내리기)은 2026-08-16 에 열렸다** — 공용 순서 ⑵(나루 `변경일지.md`).
  **판정선: 「기계가 이미 막는 절은 안 실어도 안 뚫린다」**, 그때 「막는 자」는 **훅 deny 또는
  exit≠0** 이다(세기만 하는 자는 아니다 — 실행 규율 17). 내린 절에는 **무엇을 기계가 안 막는지**와
  **읽는 법(Grep)** 을 남긴다. 첫 이관: 삽화 규격 · 콘텐츠 표기 규약.
- 옛 판정(2026-08-12 *"그대로 둬"*)은 *«쪼개도 세션 시작에 전부 로드되니 토큰을 안 아낀다»* 를 말한
  것이라 **`@import` 분할**에만 걸린다. `docs/` 이관은 실제로 안 실리므로 그 판정의 대상이 아니다.
- 설계 정본 나루 `기록/AGENTS-삭감-목록.md` · 실행 측 `docs/AGENTS-삭감.workorder.md` ·
  간결체 규약 공용 「규칙/실리는-글은-짧게.md」(실행 규율 20).


## 프로젝트 개요

한 대학 재학생의 전공 과목 학습 자료를 데이터(JSON) + 단일 뷰어 템플릿으로 만드는 프로젝트.
**어떤 과목·교재·경로인지 같은 과목별 세부는 각 브랜치의 `.claude/SUBJECT.md`에 있다** —
여기(공통)에는 과목 무관한 것만 둔다.

```
data/<과목>/chNN.json              ← 챕터 콘텐츠 (과목별)
data/<과목>/*.workorder.md         ← 작업지시서 + 진행 기록 (과목별)
site/template/viewer.template.html ← 뷰어 원본 (공유, 유일한 수정 지점)
site/_assets/viewer.<해시>.css|js  ← 빌드가 뽑아낸 공유 뷰어 (산출물, 커밋 안 함)
site/fonts/*.woff2                 ← 전 과목 공용 웹폰트 (공통, main이 정본 · 커밋한다)
site/<과목>/chNN.html              ← 빌드 산출물 (과목별)
tools/build_site.py                ← 빌드 + 내장 검사 (공유)
tools/build_review.py              ← 변경점 하이라이트 리뷰 빌드 (공유)
tools/audit_content.py             ← 읽기 전용 콘텐츠 감사 (공유)
```

**★ 뷰어는 페이지에 안 실린다 — 자리표를 새로 쓸 때만 알면 된다 (2026-08-15).**
템플릿의 `<style>`·`<script>` 중 **자리표(`{{…}}`)가 없는 블록은 빌드가 `site/_assets/` 로
뽑아낸다**(실측 근거: 페이지 335KB 중 뷰어가 143KB인데 장마다 똑같이 다시 실렸다 —
30과목 300장이면 중복만 ~42MB). 템플릿은 여전히 **유일한 수정 지점**이라 고치는 법은 안 바뀐다.
→ **딱 하나만 지키면 된다: 자리표는 첫 `<script>` 블록(`var CH = …` 가 있는 곳)에만 넣는다.**
아래 블록에 넣으면 그 블록이 통째로 페이지로 되돌아와 143KB가 장마다 다시 실린다.
왜 이렇게 갈랐는지·해시 이름이 낡은 갈래를 어떻게 살리는지는 `buildlib/render.prepare_viewer`
독스트링이 정본이고, 잠금은 `test_checks.py::test_viewer_is_split_into_shared_assets`.

## ★ 공통 vs 과목별 — main이 공통 정본 (2026-07-27 신설)

과목 = git 브랜치 + worktree. **어떤 파일이 공유되고 어떤 게 브랜치 고유인지**를 못 박지 않으면
공유 규칙이 브랜치마다 갈라진다(2026-07-27 실측: thermo 가 앞서고 main·math 는 낡아 있었다).

| 구분 | 파일 | 사는 곳 · 편집 규칙 |
|---|---|---|
| **공통 = "어떻게 일하나"** | `AGENTS.md`·`CLAUDE.md`·`.claude/**`(`SUBJECT.md`·`settings.local.json` 제외)·`tools/**`·`site/template/**`·`docs/**` | **main 이 정본** — main 에서 고치고 각 과목이 `git merge main` 으로 받는다 |
| **과목별 = "무엇을 다루나"** | `.claude/SUBJECT.md`·`data/<과목>/**`·`site/<과목>/**` | **각 브랜치 고유** — 그 과목 세션에서만. main 에 안 올린다 |

- **공통을 고쳤으면 빨리 커밋하고 즉시 main 에 반영한다.** 브랜치에만 두면 갈라진다.
  `.claude/hooks/common_guard.py` 가 공통 파일 편집 때 알린다.
- **세션 시작 때 공통이 main 보다 낡았으면** 같은 훅이 "N커밋 앞섬 — `git merge main`" 을 한 줄로 낸다.
  **경고가 뜨면 작업을 잇기 전에 먼저 merge 한다.**
- **`.claude/SUBJECT.md` 는 자기 브랜치에서 추적한다**(2026-07-26). 각 과목 `.gitignore` 를 이렇게 둔다:

  ```
  /.claude/*
  !/.claude/SUBJECT.md
  ```

  `settings.local.json` 은 계속 무시된다. **아직 안 바꾼 과목은 이 조치를 먼저 한다.**
- **`.claude/SUBJECT.md` 는 main 에 두지 않는다**(브랜치마다 달라 merge 마다 충돌한다).
  `sync_common.py` 의 `NEVER_SYNC` 가 스테이징에서 빼 기계로 막는다. 새 과목은
  `.claude/SUBJECT.md.template` 을 복사해 만든다. `CLAUDE.md` 가 `@AGENTS.md` + `@.claude/SUBJECT.md` 를 import 한다.
- **main 공통의 1차 관리 주체는 「컨테이너 루트 세션」이다**(개정 2026-08-15). 옛 규칙은 `thermo` 가
  공통을 소유했는데, 과목이 12개가 되자 그 근거가 깨졌다 — 한 과목 세션이 전 과목의 규격을 소유하게 된다.
  - 컨테이너 루트에는 과목이 없고, `guard_bash.container_root_violation` 이 **과목 콘텐츠 커밋을 막는다.**
    배선은 `sync_common.mirror_container_root()` 가 깔고 훅은 main 것을 가리킨다(사본을 안 만든다).
  - **과목 세션은 `git merge main` 으로 받는 것이 기본.** 공통을 꼭 고쳐야 하면 **먼저 merge 한 뒤**
    `sync_common` 을 돌린다 — 뒤진 채 올리면 남의 공통 작업을 덮는다(`sync_common` 이 거부한다).
    실사고 2026-07-27: math 가 뒤진 채 sync 해 thermo 의 strict 승격·guard 수정을 되돌렸다.
  - **컨테이너 루트 세션도 「과목을 선언」하면 그 과목을 만질 수 있다**(2026-08-15).
    `python main/tools/session_subject.py <갈래> --why "<사유>"` · 끝나면 `--off`. **main 의 사본으로
    부른다**(표식은 가드가 읽는 자리에 있어야 한다).
    - **완화가 아니다.** 가드가 막던 이유는 허락이 없어서가 아니라 **이 세션에 과목 정체가 없어 어느
      파일이 정당한지 기계가 못 가려서**다. 나머지 과목은 선언 뒤에도 그대로 막힌다.
    - **선언 중에는 공통을 못 만진다 — 배타다.** 공통을 고치려면 선언을 먼저 끈다.
    - **세션에 묶인다.** 표식에 세션 id 가 박히고 다른 세션에서는 무시된다(12시간 만료).
    - 잠금 `test_subject_declaration_restores_the_boundary`.
    - **★ 기본값은 「내가 직접 선언해서 한다」다**(2026-09-03 사용자 판정 — 별도 지시가 없으면 에이전트가 각 과목 세션에 직접 들어가 처리하는 것이 기본이고, 다른 세션에 맡기려면 사용자가 그때그때 명시해야 한다). 과목 콘텐츠를 고쳐야 하는 지시를 받으면, 그 과목의 **실제 사용자 세션에 메시지를 보내 대신 해 달라고 부탁하지 않는다** — 직접 「session_subject.py <갈래>」로 선언하고 고치고 커밋하고 끈다. 사용자가 특정 과목 세션으로 **명시로 라우팅을 지정할 때만** 메시지로 넘긴다. 실행 규율 15(다른 세션에 메시지 보내는 선)의 「정당한 자리 셋」과 같은 결의 판정이다 — 기본은 파일·직접 작업, 메시지는 예외.
  - 컨테이너 루트에서는 `git -C <워크트리>` 와 `python <워크트리>/tools/…` 가 정상이고 필수다.
  - **새 판정을 쓸 때 「세션은 워크트리에 선다」는 전제를 먼저 의심한다** — 배선을 켠 날 그 전제에서
    넷이 한꺼번에 터졌다(경계 꺼짐 · `main/tools/…` 가 리포 밖 · `git -C main` 이 잡음 ·
    `session_brief` 가 bare HEAD 를 읽어 오보).
- **세션이 열릴 때 「선 자리」를 훅이 찍는다** — `.claude/hooks/session_brief.py`(2026-08-14).
  갈래·과목·최근 인박스/워크오더 경로만 낸다. **판정은 하지 않는다.** 열린 이유: 폴더 이름으로 목표를
  짐작해 엉뚱한 프로젝트의 할 일을 뽑은 사고가 두 번. 잠금 `test_session_brief_states_facts_not_guesses`.
- **신선도 훅은 SessionStart + `UserPromptSubmit`(매 턴)에서 검사한다**(2026-07-27 확장). 세션 도중 남이
  main 에 올려도 다음 턴에 경고가 뜬다(낡았을 때만 한 줄 → 평소 토큰 0). `sync_common` 이 push 직전
  behind 를 거부해 이중으로 막는다.

## 절대 규칙 (위반 시 파이프라인/원칙 붕괴)

1. **빌드 산출물 손편집 금지.** `site/*/**.html` 은 빌드가 덮어쓴다. 뷰어 수정 =
   `site/template/viewer.template.html`, 콘텐츠 수정 = `data/**/*.json`, 그 뒤 `python tools/build_site.py`.
   검사 실패는 데이터·템플릿을 고쳐 통과시킨다(검사 완화 금지).

2. **콘텐츠 무단 창작 금지(no-fabrication).** 이론·공식·문제·pitfall 은 교재 대조 또는 오답로그·사용자
   제보가 근거여야 한다. `pitfalls[].source` 는 `"Cengel 본문 경고 (...)"` · `"오답로그 e0NN"` ·
   `"사용자 제보 (날짜)"` 3종만(빌드가 검사한다). 근거를 확인할 수 없으면 만들지 말고 먼저 묻는다.

3. **교재 원문 저장 금지(저작권).** `error_log.json` 에는 문제 번호 참조와 진단만. 교재 문장을 데이터에
   복사하지 않는다(자작 요약·변형만).

4. **수치는 스크립트로 독립 검산.** 답·중간값을 눈으로 계산해 적지 않는다.

5. **홈의 Claude 메모리(`C:\Users\<사용자>\.claude\**`)는 건드리지 않는다.** 프로젝트 안의 `.claude/` 는
   대상이 아니다.

6. **`git push` 는 배포가 아니다.** Netlify→Vercel 전환(2026-08-31) 뒤에도 자동 배포는 없다 —
   Vercel 프로젝트를 GitHub 연동 없이 만들었고(`vercel git disconnect` 로 확인됨), 배포는
   `tools/deploy_all.py` 가 찍는 `vercel deploy` 수동 실행뿐이다. 프로덕션은 `--prod` 플래그가
   있어야만 돌고 **사용자가 명시적으로 요청할 때만** 쓴다(플래그 없는 프리뷰 배포는 무료).
   "push 했으니 게시됐다"는 오보다. 로컬 커밋·push 는 자유롭게 해도 된다 —
   단 「자유롭게」는 GitHub 자동 배포가 꺼져 있다는 사실에 걸린 말이라 옮길 때 다시 잰다.
   언제 미느냐의 판정은 실행 규율 14 가 정본이다.

7. **지적은 「부류」로 고친다 — 인스턴스만 고치는 것은 위반이다.**
   ⑴ 왜 그렇게 지시했는지 원칙을 먼저 뽑고 ⑵ 그 원칙에 걸리는 같은 부류 전체를 전 챕터에서 감사해
   ⑶ 한꺼번에 고친다. 똑같은 상황이 아니라 비슷한 케이스도 같은 논리로 적용한다.
   - **기본값은 부류다 — 인스턴스는 증명해야 하는 예외다**(2026-08-07 사용자 재지적). 순서:
     ⓐ "이건 무슨 부류인가"를 말로 적는다 ⓑ 그 부류를 전 챕터에서 센다 ⓒ 한 건뿐이고 구조상 더 나올
     수 없을 때만 인스턴스로 처리한다.
   - 인스턴스로 처리했으면 그 판단을 밝힌다 — "부류로 봤으나 N건 중 1건이고 재발 경로가 없다".
     밝히지 않으면 읽는 사람은 부류를 안 본 것과 구별할 수 없다(규칙 11).
   - **재발이면 원인과 방지장치까지 답한다.** 과거 커밋·워크오더·`legacy*` 잔재에 흔적이 있으면 고치는
     것으로 끝내지 말고, 왜 또 났는지와 기계가 막는 조치(빌드 lint 승격 · 기본값 뒤집기 · 레지스트리화)를
     함께 낸다. "다음엔 주의"는 조치가 아니다. 원인은 "빠뜨렸다"가 아니라 **빠뜨려도 통과되는 구조**를
     지목해야 한다.
   - **구현 전 체크포인트** — 워크오더나 첫 진행 보고에 한 줄씩:
     `사용자가 든 사례` → `실제 원하는 결과` → `동일 부류 감사 범위` → `재발 여부·구조적 원인` →
     `제안안 평가·더 나은 대안` → `이번에 추가할 기계적 방지장치`. 해당 없음도 이유를 쓴다.
   - 예: "삽화 글자가 작다" → 폰트가 아니라 원인(viewBox 축소)을 찾아 전 삽화를 감사.
     "화살표가 선 끝과 안 맞는다" → 끝점이 맞아야 하는 모든 도형의 좌표를 맞춘다.
   - 선례(2026-07-21): 이해도 체크가 `theory.sections` 만 순회해 유도 11건이 같은 결함으로 통과했다 →
     `_iter_check_owners` 로 넓히고, 필드가 여러 컬렉션에 사는지를 `tools/audit_content.py` 가 상시 감시한다.
   - 새 챕터·검수 때는 `data/*/chapter-quality-contract.md` 를 함께 읽고 **모든 챕터**에 같은 기준이
     계승됐는지 확인한다. 과거 워크오더의 "완료" 문구로 판정하지 말고 파일을 직접 본다(미반영 전례 2회).
   - **부류 판정은 과목 경계를 넘는다** (2026-08-29). 처방(수정)이 삽화·표기·폰트·규칙 중 **어느
     소재든, 뷰어·빌드처럼 전 과목이 공유하는 「만드는 방식」의 상수·규칙**이면, "메인에 올릴 만큼
     크냐"를 재지 않고 그 자리에서 공통 도구(`tools/fix_*.py`·`buildlib/checks_*.py`)나 공통 문서
     (`docs/*-규격.md`)로 올린다. "이 과목만의 사정"이라는 판단은 그 처방이 **과목의 사실**(교재
     내용·범위)을 다룰 때만 유효하다 — 크기가 아니라 종류로 가른다.
     - **⑵(공통 도구·문서 신설)를 만드는 동시에 ⑶(드리프트 체크)도 만든다.** 새 도구는 앞으로
       쓸 때만 막고 **이미 존재하는 다른 과목의 낡은 콘텐츠는 안 본다** — 그건
       `tools/audit_convention_drift.py` 에 그 규칙을 재는 체크 함수를 등록해야만 걸린다
       (예: 열역학이 실측한 삽화 여백 기준). 등록을 빠뜨리면 새 규칙은 미래만 지키고 과거는
       방치된다.
     - **도구가 안 만들어진 인스턴스는 이 조항의 대상이 아니다.** 1회성으로 판정해 도구화를
       안 한 것은 위 "기본값은 부류다" 절차가 이미 처리한다 — 진짜 1회성이면 두 번째 발생이
       없으므로 소급할 대상 자체가 없다. 소급이 필요한 것은 **⑵는 있는데 ⑶이 없는 경우**뿐이고,
       그 목록은 도구 등록부(`tools/` 절)에 이미 유한하게 나열돼 있어 커밋 로그를 새로 뒤질
       필요가 없다.
     - **⑷ AGENTS.md·CLAUDE.md 의 트리거 줄도 같은 자리에서 넣는다 — ⑵·⑶은 「이미 쓴 것」만
       본다, 「지금 쓰는 중」은 못 본다.** ⑶(드리프트 체크)은 다 만들어진 콘텐츠를 사후에
       훑는 감사라 **능동적으로 새로 쓰는 중인 세션은 못 잡는다** — 실사고(2026-08-29):
       슬라이드모드 사양이 2026-08-18에 `docs/`에 생겼는데 AGENTS.md 가리키는 줄은
       2026-08-29에야 들어갔고, 그 사이(8/18~8/29) 응용고체역학이 **새 챕터를 쓰던 중**이라
       사양을 몰라서 못 따랐다 — 감사로는 이 종류를 못 막는다, 쓰는 순간에 알아야 막는다.
       그래서 ⑵를 만들면 **그 자리에서** AGENTS.md·CLAUDE.md에 "이거 읽고 만들어라" 트리거
       한 줄을 반드시 같이 넣는다(문서 배치 원칙 「올릴 자격」과 같은 자리 — 필요한 절만
       가리키게 하지 통째로 싣지 않는다).

8. **지적을 무작정 수용하지 않는다 — 분류하고 입장을 밝힌다.** 사용자 지시는 대개 *기존에 사용자 요구로
   만든 것*을 고치라는 것이라, 받아적기 전에 검토한다.
   - 분류: ⑴ **결함**(깨짐·오류·물리적으로 틀림) → 즉시 수정 ⑵ **설계 판단**(무엇을 보여줄지·배치·기능)
     → 입장 표명 의무 ⑶ **취향** → 대안 제시 후 사용자 선택.
   - 배치 끝에 카운트 보고: "N건 중 결함 X · 설계판단 Y · 반박 Z". **설계 판단이 있는데 반박 0이면
     경고 신호**다 — 왜 전부 타당했는지 해명한다.
   - 전제: 큰 배치가 오면 속기 모드에 빠져 판단이 밀려난다. 목적은 완결성이 아니라 검증이다.

9. **쓰기는 허용된 두 곳에만**(권한 규칙이 못 막는 마지막 층).
   - 쓰기 = ⑴ 프로젝트 루트 안 ⑵ 에이전트 전용 세션 스크래치패드. 그 외에는 어디에도 쓰지 않는다 —
     바탕화면·내 문서·홈·교재 폴더 전부.
   - 읽기 = 프로젝트 루트 + 교재 폴더(`additionalDirectories`) + 스크래치패드.
   - 경로가 어떻게 생겼든 적용된다: 리디렉션 · 출력 경로 인자(`pdftotext … 경로`, `sort -o 경로`) ·
     파이썬 `open(경로,"w")`.
   - 권한 규칙으로 안 되는 이유: 리디렉션은 명령이 아니라 셸의 성질이라 규칙은 `>` 대상이나 `open()` 을
     못 본다. **그래서 이 규칙이 실질 경계다 — 규칙이 통과시켰다는 게 해도 된다는 뜻이 아니다.**
   - 두 곳 밖에 써야 하면 하지 말고 먼저 묻는다.

10. **할 수 있는 것/없는 것은 에이전트가 먼저 선언한다.** 작업을 받으면 착수 전 첫 응답에서 3구역으로
    분류해 선언한다. 형식: `구역: 초록 N건 / 노랑 N건 / 빨강 N건 — <빨강 항목과 이유>`.

    | 구역 | 범위 | 행동 |
    |---|---|---|
    | **초록** | SVG 좌표·CSS·리팩터·빌드 스크립트 확장·감사 실행·빌드가 이미 지적한 것 수정 | 그냥 한다 |
    | **노랑** | 부류 범위 판단, 결함/설계 분류, 배치·레이아웃 선택 | 한다. 단 `## 자율 판단 기록`에 남긴다 |
    | **빨강** | 출처 없는 콘텐츠 창작, 콘텐츠 삭제, 배포·공개 설정 변경, `error_log.json` 수정, 검사 완화·우회 | 하지 않는다. 못 하는 이유와 필요한 것을 적는다 |

    - 콘텐츠 빨강의 기준은 「출처가 있는가」다: 출처 없는 창작은 빨강, **교재 대조를 마친 작성은 빨강이
      아니다**(이론·유도·문풀·삽화 전부 내 몫). 삭제·배포·`error_log.json`·검사 완화는 빨강이다.
    - 노랑의 승인자는 사용자다. 그 자리에서 받으면 `(사용자 승인: 날짜/출처)` 로 기록하고 진행한다.
      사용자 부재가 작업을 멈추는 이유가 되어선 안 된다.
    - 자기 구역 밖 작업을 조용히 진행하지 않는다.

11. **판정은 명령으로 뒷받침한다 — 미검증 주장을 검증된 것처럼 쓰지 않는다.** "통과"·"N건 전부 확인"·
    "이상 없음" 에는 그것을 산출한 명령을 함께 적는다.
    - 명령을 댈 수 없으면 `육안 확인(미검증)` 이라고 표시한다. 미검증과 검증이 겉모습이 같으면 읽는
      사람은 구별할 방법이 없다.
    - 수량 주장은 스크립트 출력에서 온 것이어야 한다. 세지 않고 "대부분"·"1위"·"압도적"을 쓰지 않는다.
    - 감사를 근거로 쓸 때는 **그 스크립트의 순회 범위부터 확인한다.** 범위를 모르는 "0건"은 "없다"가
      아니라 "거기까지는 없다"이다.
    - 전제(2026-07-21 하루 4회): 승인 프롬프트 1위 오단정 · 이론만 순회한 결과로 17건 통과 판정 ·
      규칙 도달 여부 미확인 · `verify_workorder` 를 읽기 전용이라고 기술. 넷 다 확인 가능한 것을 확인하지
      않고 단정했다 — 기억이 아니라 절차의 문제다.
    - **명령을 붙여도 틀리는 자리 — 「자」부터 의심한다**(2026-08-14, 하루에 세 번 그럴듯하게 틀렸다):
      - 새 자의 첫 실행은 「재는 것」이 아니라 「자를 재는 것」이다. 첫 출력을 결론으로 쓰지 말고
        *이 자가 무엇을 못 보나* 를 먼저 적는다.
      - 순회가 잘렸는지 먼저 본다. 상한에 걸린 「0건」은 「거기까지는 없다」이다.
      - 같은 값을 두 가지 방법으로 재 본다. 한 자만 쓰면 틀렸을 때 알 방법이 없다.
      - 명령으로 안 닫히는 넷은 사람이 본다: 뷰어 JS·CSS 동작 · 삽화 렌더 · 리포 밖 파일 비교 ·
        「결과가 그럴듯해서 그냥 넘어가고 있지 않은가」. 자리를 비우고 목표만 남기는 운용(`/loop`)일수록
        마지막 항목이 커진다.
    - 기계로 검사할 수 있는 것은 검사로 만든다(빌드 검사·`tools/audit_content.py`). 없는 것은 미검증이라고
      밝힌다. 둘 중 하나여야 하고 조용한 단정은 없다.

12. **독자성 — 이 자료는 번역본도 요약본도 아니다.** 교재는 사실 확인용이지 서술의 원본이 아니다
    (2026-07-22 사용자 지적: 2장이 원서와 너무 닮았다).
    - **⑴ 절 구성은 교재 목차가 아니라 학습 순서로 짠다.** `chNN.textbook-map.md` 는 대조용 지도이지
      목차 원본이 아니다. 절 순서가 교재와 1:1이면 본문이 닮기 쉬우니 그 자리를 들여다본다.

      | 대상 | 방침 |
      |---|---|
      | **장(chapter) 순서** | 교재를 따르는 것이 정상이다. 감사 대상이 아니다 |
      | **절(section) 순서** | 학습 순서로 짠다. 바꿀 이유가 있으면 바꾸고, 없으면 두는 것이 맞다 |

      - **위반은 「같은 순서」가 아니라 「닮은 서술」이다.** 순서 1:1은 어디를 들여다볼지 알려주는 신호라
        감사도 `[FAIL]` 이 아니라 `[판정 필요]` 로 찍는다(정정 2026-08-02, 사용자).
      - 의존 관계가 순서를 정하는 곳에서는 학습 순서가 곧 교재 순서다(고체역학 ch02 · 동역학 ch12).
      - **그때는 에이전트가 사용자에게 반박해야 한다.** 규칙을 기계적으로 적용해 절을 흔드는 것은 규칙 8이
        금지하는 속기 모드의 다른 얼굴이다 — 감사를 조용히 시키려고 절을 옮기면 독자만 손해를 본다.
      - **세 조치를 구분한다. 감사 지표는 그중 하나만 본다.**

        | 조치 | 예 | 인용 순서 지표가 바뀌나 |
        |---|---|---|
        | **쪼개기·합치기·경계 옮기기** | 고체역학 ch01·ch02 · 동역학 §6·§10 | 아니오 — 오름차순 유지 |
        | **교재에 없는 절 신설** | 동역학 §3·§7 | 아니오 — 대응 절이 없다 |
        | **순서 바꾸기** | 열역학 | 예 |

        재구성의 정본 형태는 첫 줄이고 순서 바꾸기가 가장 드물다. `audit_content` 5a 의 1:1 신고는 셋째만
        재므로, 신고를 보고 절을 흔들기 전에 위 표로 무엇을 이미 했는지 센다.
        도구는 `tools/reorder_sections.py`(순서만 바꾼다 — 내용·상호참조는 사람이 고친다).
      - 유지하기로 했으면 그 챕터 `chNN.textbook-map.md` 에 `## 순서 판정` 절을 두고 ⑴ 무엇을 검토했나
        ⑵ 왜 유지했나 ⑶ 독자성을 무엇으로 확보했나를 적는다. 기록이 없으면 계속 신고된다.
      - 순서는 독자성 확보 수단 중 하나일 뿐이고 **비용이 가장 크다**(옮기면 앞 절을 전제한 문장이 전부
        거짓말이 된다).
    - **⑵ 예시 소재는 자체 발굴한다.** 교재가 든 사물·상황을 그대로 쓰지 않는다.
    - **⑶ 삽화는 교재 그림을 재현하지 않는다.** 구도·라벨·수치가 교재 Figure 와 일치하면 위반이다.
    - **⑷ 연습문제는 숫자만 바꾼 것이 아니어야 한다.** 상황·묻는 것 중 최소 하나가 달라야 한다.
    - **⑸ Moran 을 실제로 쓴다.** 같은 내용이면 Cengel, Moran 이 낫거나 Cengel 에 없으면 Moran 을 본문에
      쓴다. `supplementNotes` 에 "두 책 차이" 메모만 쌓는 것은 반영이 아니다(2026-07-22 실측: ch02 의
      Moran 언급 18건 중 이론 본문은 1건).
    - 전제: 규칙 2·3만으로는 못 막는다 — 이론 본문이 교재를 닮는 것을 벌하는 규칙이 없어서, 대조를
      성실히 할수록 번역본에 수렴하는 구조였다.
    - 기계: `tools/audit_content.py` 가 ⓐ 절 순서와 교재 절 순서의 1:1 ⓑ 이론 본문의 Moran 출처 0건을
      감시한다.

## 내부 링크(교차 참조) 규칙

> 신설 2026-07-22. 문법은 `[[chNN:앵커id|표시할 문구]]`.
> **과목 간**은 `[[과목폴더@chNN:앵커id|표시할 문구]]`(신설 2026-08-29, 예:
> `[[고체역학@ch09:sec-bending|9장 굽힘응력]]`). `@` 앞은 그 과목의 **폴더 이름**이다(브랜치 이름이
> 아니다 — `data/<폴더>/`). **대상은 이 빌드가 검사하지 못한다**(다른 git 브랜치라 못 건너간다) —
> 빌드가 경고만 내고, 실존 여부는 저자가 `git show <브랜치>:data/<과목>/chNN.json` 로 확인한다.
> **쓰는 자리는 1학기→2학기 되짚기뿐이다** — 앞서 배운 것을 다시 보여줄 때만 쓰고, 안 배운
> 내용을 미리 보여주는 자리에는 쓰지 않는다(과목별 정본은 그 과목 `.claude/SUBJECT.md`).

- **링크는 독자가 기대한 것이 화면에 바로 보이는 곳에 착지해야 한다.** "정상유동 과정"을 눌렀는데
  그 개념이 아니라 상위 절의 맨 위가 보이면 결함이다. **절 id는 최후의 수단**이고,
  삽화·유도·정의 문단처럼 **더 좁은 앵커가 있으면 반드시 그쪽을 가리킨다.**
- **착지할 만한 대상(삽화·정의·유도)이 없을 때만 절 링크를 쓴다.** 그 경우 표시 문구도
  `'과정과 사이클' 절`처럼 **절로 간다는 것이 드러나게** 적는다.
- **같은 화면에 같은 대상으로 가는 링크를 두 번 걸지 않는다.** 중복이면 **본문 쪽을 남기고
  삽화 캡션 쪽을 뺀다** — 용어의 첫 등장은 본문이고, 삽화는 교체·삭제될 수 있기 때문이다.

## 실행 규율 — 승인 피로와 토큰 낭비 막기

> 여기에는 **규칙·판정선·전제만** 둔다. 경위·실측은 원장(나루 `기록/feedback-ledger.md`,
> 조회 `python tools/feedback_lookup.py "<요지>"`)과 각 도구 독스트링이 정본이다.

1. **지적은 모았다가 한 번에 처리한다.** 즉시 반영하지 말고 수집 목록에 쌓아, 사용자가 "해줘" 할 때
   감사 → 수정 → 빌드를 한 번에 돈다.
   - 전제: 지적 1건마다 같은 부류 전수 감사가 붙는다(규칙 7) — 10건을 따로 주면 10번 돈다.
   - 사용자는 발견 즉시 전달해도 된다. 배치는 에이전트가 지는 책임이지 사용자에게 참으라는 규칙이 아니다.
   - 명시적 실행 요청이나 검수 종료 전까지 감사·수정·빌드를 시작하지 않는다.
   → 원장 2026-07-27 「지적은 모아 한 번에」.

2. **감사는 하나로 합쳐 한 번만 돌린다.** 필요한 감사를 먼저 전부 설계한 뒤 한 스크립트로 실행한다.
   일회성 `python - <<EOF` 를 쪼개 반복하지 않는다(임의 코드 실행은 매번 승인).
   또 쓸 것 같으면 고정 도구로 승격한다(`tools/audit_content.py` 선례).
   → `.claude/hooks/guard_bash.py` 의 `deny_reason()`.

3. **착수 전 예상 승인 항목과 횟수를 보고한다.** 예상 밖 임의 스크립트가 필요해지면 실행 전에 설명하고
   승인받는다. 목적은 사용자가 잔량과 대조해 작업을 자를 수 있게 하는 것이다.

4. **정확 허용된 명령에 리디렉션·후처리를 붙이지 않는다.** `2>&1` 같은 리디렉션은 정확 규칙과 다르게
   평가된다. 승인이 떴으면 승인창에 실제로 걸린 하위 명령을 확인하고 추측으로 단정하지 않는다(규칙 11).
   - **파일 편집도 배치로.** 한 파일의 여러 곳을 `Edit` 로 나눠 부르지 말고 한 번의 `Write` 나 최소
     횟수의 편집으로 몰고, 끝난 뒤 한 번만 보고한다. → 원장 2026-07-27 「한 파일을 여러 번 편집」.
   - `Write` 가 한 턴 출력에 안 들어갈 문서는 `python tools/replace_doc_section.py` 로 절 단위 교체
     (잘린 `Write` 는 파일을 깨뜨린다).

5. **위 1~4는 `.claude/hooks/guard_bash.py` 가 막는다.** 차단 목록과 각 규칙이 열린 경위는 그 파일
   `deny_reason()` 이 정본이다 — 여기 복사하지 않는다(중복은 갈라진다).
   - 새 스크립트를 짜기 전에 `tools/` 의 기존 도구가 그 검사를 하는지 확인한다.
   - 훅이 거부하면 형태를 바꿔 다시 짠다. 끄거나 우회하는 것은 빨강(규칙 10).
   - 잠금은 권한이 아니라 테스트다 — `test_checks.py::test_guard_rules`. 거부 규칙을 추가하면 케이스도
     추가하고 판정은 전부 `deny_reason()` 안에 둔다(`main()` 에 쓰면 테스트가 못 본다).

6. **토큰 걱정으로 작업을 잘게 끊지 않는다 — 길게 이어서 진행한다.**
   > 전제: 현재 max 플랜이라는 **사실**에 걸린 조항이다. 한도가 좁은 환경으로 옮기면 조항이 아니라
   > 이 전제를 먼저 다시 잰다(사용자는 다음 달 낮은 요금제로 옮길 예정).
   - "한 번에 쭉"·"계속 진행"이라 했으면 매 단계 되묻지 말고 배치를 끝까지 민다.
   - 끊어도 되는 것은 ⑴ 사용자 결정이 실제로 필요한 설계 분기 ⑵ 턴 출력 한계에 실제로 닿았을 때뿐이다.
     "토큰이 걱정돼서"는 사유가 아니다.
   - 1~4(승인 배치·감사 통합)는 그대로 유효하다 — 승인·낭비를 줄이는 것이지 작업을 쪼개라는 뜻이 아니다.
   - `/loop` 안에서도 같다. `delaySeconds` 60초는 다음 깨어남까지의 최소 간격이지 한 턴의 상한이 아니다.
     - ★ `ScheduleWakeup` 도구 자신의 일반 안내(신호 없는 유휴 틱 20~30분)를 따르지 않는다 —
       그 안내는 *외부 신호를 기다리는* 경우의 것이다. 기다리는 대상이 없으면 60초로 고정한다
       (2026-08-29 세 번째 재발). → 나루 `규칙/끊지-말고-끝내고-보고한다.md`.
     - 판정선 ⑴ — 회차는 순서 번호 하나를 닫기 전에 끝내지 않는다(워크오더·루프 프롬프트의 번호가 단위).
     - 판정선 ⑵ — 진단만 하고 끝내지 않는다. 원인을 찾았으면 그 회차 안에서 처방까지 간다.
     - 트리거는 `ScheduleWakeup` 이다 — 부르는 자리에서 ⑴⑵ 를 자문하고, 아니면 예약하지 말고 계속한다.
     - 회차마다 보고하지 않는다 — 회차 중에는 한 줄 상태, 기록은 인박스·커밋 메시지가 정본, 표 보고는
       루프가 끝날 때 한 번. 재예약 프롬프트에는 정본 파일과 다음 항목만. 빌드는 `--quiet` 가 기본이다.
     → 원장 2026-07-30 · 2026-08-12 · 2026-08-29 「루프 회차 작업량」.

7. **반복되는 승인 프롬프트는 기계로 고친다.** 같은 명령이 새 세션마다 뜨면 기억이 아니라 설정·가드의
   문제다. 원인을 실측해 규칙을 바꾼다.
   - 읽기 전용 git → `guard_bash.SAFE_GIT` 에 추가. 일상 로컬 쓰기(add·commit·merge)는 과목 브랜치에서
     자동 허용된다(다른 과목 경로는 `deny_reason` 이 막는다).
   - `checkout·restore·reset·rebase·push` 는 게이트 유지. 단 `push` 의 근거는 「게시라 못 되돌린다」
     하나라 **비공개 원격에서는 성립하지 않는다** — 언제 미느냐는 규율 14 가 정본이다.
   - 미커밋 변경 되돌리기는 `python tools/revert_files.py <경로…>` — `diff --stat` 을 찍고 지금 내용을
     스크래치패드에 남긴 뒤 HEAD 로 복원한다. 게이트의 근거 자체를 없앤 것이라 완화가 아니다.
     아는 것은 `checkout -- <경로>` 뿐이고 브랜치 전환·리셋·강제·`.`·`-A` 는 거부한다.
   - 커밋은 `python tools/commit.py "<메시지>" <경로…>`, 공통 반영은 `python tools/sync_common.py` —
     둘 다 git 을 subprocess 로 돌려 승인을 안 탄다. 손으로 `git checkout … 파일나열` 하지 않는다.
   - `commit.py` 는 공통 파일이 섞이면 `sync_common` 을 스스로 돌린다. 무엇이 공통인지는
     `sync_common.common_paths_in()` 하나가 판정하고 끄는 깃발을 두지 않는다 — 싫으면 커밋을 쪼갠다.
     `sync_common` 은 공용 시스템 폴더(나루)까지 밀고, 대장은 `docs/shared-mirror.txt`, 공용 쪽이 우리
     모르게 바뀐 파일은 덮지 않고 신고한다.
   - 프롬프트가 뜬 명령은 고치기 **전에** `docs/permission-prompt-ledger.md` 에 한 줄(기록이 없으면
     재발인지 첫 발생인지 구별할 수 없다). 「검증」엔 다시 실행해 본 결과만 적는다.
     새 세션 시작 직후 그 원장 맨 아래 '반드시 할 확인'을 실행한다.
   - blocking `PreToolUse` 훅은 permission `allow` 보다 먼저 평가되고 훅 deny가 우선한다.
     리포 도구는 경로만 보지 않고 **HEAD 추적·blob 일치**까지 확인한 뒤에만 자동 허용한다.

8. **기록은 보고가 아니다 — 사용자에게 직접 보여야 끝난다.** 워크오더·원장은 에이전트가 스스로를
   검증하는 장치이지 사용자의 읽을거리가 아니다.
   - 긴 자동 실행(loop·배치)이 끝나면 요약을 낸다: ⑴ 사용자 결정이 필요했던 것과 내가 내린 판단
     ⑵ 확인하실 자리(절·삽화 id) ⑶ 남은 것.
   - 「확인하실 자리」에는 **지금 검수하는 범위 안의 것만** 올린다. 범위 밖 수정은 「고쳤음」 쪽에만
     적는다 — 안 본다고 한 것을 보라는 뜻이 되기 때문이다. 판정 기준은 뷰어와 같다: 화면에서
     하이라이트를 안 보여 주는 컬렉션은 보고에서도 대상이 아니다. → 원장 2026-08-07.
   - 「되돌리는 법」은 보고에 넣지 않는다(요청받은 적이 없다). `## 자율 판단 기록` 파일의 것은 그대로
     둔다. 부류: 지적에 없던 항목을 끼워 넣으면 요구였던 것처럼 굳는다 — 조항을 지워야 고쳐진다.
     → 원장 2026-08-05.
   - 중요도를 시각적으로 구분한다 — 판단·결함·되돌릴 수 없는 것은 굵게·표·별도 절. 평평한 목록은
     읽는 사람이 경중을 못 가리고, 그건 쓴 사람의 책임이다.
   - 「무엇을 바꿨나」에는 「어느 부류 때문인가」를 붙인다: `부류 → 사용자 지적 → 무엇을 바꿨나 → 어디`
     표. 여러 부류를 연달아 닫았으면 누적된 부류를 전부 담는다.
   - 「닫았다」고 쓴 줄에는 그것을 산출한 명령을 붙인다: `항목 | 증거 명령 | 실제 출력`. 못 대면 닫지
     말고 `미검증` 으로 열어 두고, 부분만 봤으면 본 만큼의 분수를 적는다(반올림 금지).
     이 조항에는 기계 게이트가 없다 — 기계가 보는 자리는 `verify_workorder` 의 `## 완료 기준`이다.

9. **루프 재예약은 훅이 감시한다 — 손으로 기억하지 않는다.** 켜기·끄기만 사람이 한다
   (`python tools/wakeup_guard.py on` · `off` · `status`). 루프 모드인데 그 턴에
   `ScheduleWakeup` 이 없으면 Stop 훅이 멈춤을 막는다 — 끝낼 것이면 `off` 를 먼저.
   깃발이 꺼져 있으면 아무 일도 안 하고 12시간 뒤 만료된다. ★ **PC 는 안 끈다**(2026-08-26 은퇴).
   잠금 `test_wakeup_guard_blocks_a_turn_without_a_wakeup`. → 원장 2026-08-07 · 2026-08-26.

10. **의존이 없는 도구 호출은 한 응답에 묶어 부른다.** 왕복 한 번마다 그때까지의 문맥이 통째로 다시
    들어간다.
    - 묶는다: 서로 결과를 안 보는 `Grep`·`Read`·`Glob`, 서로 다른 파일의 편집, 독립된 검사 실행.
    - 못 묶는다: 앞 결과가 다음 인자를 정하는 것 — 거기까지 묶으면 틀린 일을 빠르게 하는 것이다.
    - 묶음률을 목표 숫자로 삼지 않는다. 자는 `tools/audit_tool_parallelism.py`.

11. **게이트는 배치 끝에 한 번 돌린다 — 편집마다 돌리지 않는다.**
    - 검사를 줄이라는 것이 아니라 시점을 묶으라는 것이다. 완화·우회는 빨강(규칙 10)이고, 한 번도 안
      돌리는 것은 미완이다.
    - 자리: `build_site.py` 는 데이터·템플릿을 고친 배치 끝에, `test_checks.py` 는 검사·훅·도구를
      건드렸을 때, `close_report.py` 는 세션을 닫을 때 한 번.
    - 예외는 스스로 증명한다(깨지기 쉽거나 여러 파일에 걸친 수정). 금지가 아니라 기본값이다.
      → `tools/close_report.py` 독스트링.
    - 기계: `.claude/hooks/gate_rerun_guard.py` 가 같은 게이트의 재실행을 막는다
      (배선 확인 `python .claude/hooks/gate_rerun_guard.py --check-wiring .`).

12. **비싼 것은 왕복 수가 아니라 「도구 출력의 크기」다.** 한 번 들어온 큰 출력은 그 뒤 모든 왕복에서
    다시 읽힌다 — 1만 자를 한 번 뱉고 20번 더 대화하면 20만 자다. 규율 10 보다 이쪽이 크다.
    - 처방 셋: ⑴ 파일을 통째로 읽지 않는다(`Grep` 으로 필요한 줄만, 창은 `-o` 로 좁게)
      ⑵ 감사·빌드는 `--fail-only`·`--quiet` ⑶ 바깥 문서는 필요한 값만 뽑아 온다.
    - **좁은 창은 「읽었다」가 아니라 「본 만큼」이다.** 표 한 행의 마지막 칸에 지시가 오는 형식에서
      앞 칸만 보고 「확인 완료」로 표시하면 지시가 통째로 사라진다 — 표시 전에 그 줄의 끝 칸을 봤는지
      자문한다. → 원장 2026-08-16.
    - 스크린샷은 마지막 수단이다(1장 ≈ bash 354회). 그림이어야만 하는 것은 예외다.
    → 원장 2026-08-12 「도구 출력 크기」(96.8% 가 `cache_read`).

13. **서브에이전트는 공짜 병렬이 아니다.** 서브에이전트마다 자기 시스템 프롬프트와 툴 정의를 매 요청에
    다시 보내고, 동시에 돌면 캐시가 어긋난다 — 실측 2.6~5.9배, 공식 문서는 plan 모드 팀에 약 7배를
    적는다. 이 리포는 사용자가 요청할 때만 쓴다. 값을 하는 것은 범위가 크고 서로 독립인 하위 작업뿐이다.
    > 전제: 토큰 2.6~7배는 구조에서 오므로 어디서나 참이지만, 「그래서 안 쓴다」는 판정은 **병목이
    > 검수자 한 명의 시간**이라는 사실에 걸려 있다 — 리뷰어가 여럿인 곳에서는 깨진다. 병목을 먼저 잰다.
    - correctness-first, not speed-first. 안 돌린 게이트는 낭비가 아니라 미완이고, 규율 11 로 게이트를
      미루지 않는다.

14. **커밋·푸시는 묻지 말고 하고, 한 줄로 사후보고한다.** 「제안한다」도 묻는 것이다(제안은 결국 답을
    기다린다) → 판단하고 · 실행하고 · 보고한다.
    - 하기 전에 묻는 자리는 넷뿐이다. 판정선은 「이것이 나 말고 다른 사람에게 보이나」:
      ⑴ 원격이 공개(`gh repo view --json isPrivate`) ⑵ 다른 사람이 그 갈래를 쓴다
      ⑶ 되돌릴 수 없는 조작(`push --force`·히스토리 재작성·태그·릴리스·브랜치 삭제)
      ⑷ 밀면 무언가 자동으로 돈다(배포 훅·CI·과금).
      근거는 비용이 아니라 **내가 못 되돌려서**다 — 크기로 잡으면 매번 「이 정도면 크지 않다」로
      통과된다. 묻더라도 배치가 끝날 때 묶어서 한 번.
    - 안 바뀌는 것 셋: 커밋 메시지의 「왜」(기록이 유일한 설명이다) · 게이트를 먼저 통과시키는 것 ·
      미커밋 변경을 버리는 조작은 그대로 게이트다. 이 규약은 남기는 쪽을 쉽게 하는 것이다.
    - 보고는 사유와 함께 — "원격이 공개라 못 되돌려서 안 했습니다". 사유 없는 부정 보고는 판정할 수 없다.
    → 정본은 공용 「규칙/커밋-푸시-규약.md」. 절대 규칙 6 의 "자유롭게"는 이 프로젝트의 사실
    (Vercel GitHub 자동 배포가 꺼져 있음)에 걸린 말이라 옮길 때 먼저 다시 잰다.

15. **다른 세션에 메시지를 보내는 선 — 기본은 파일이다.** 파일은 무시할 수 있고 메시지는 무시할 수
    없다. 파일 한 줄은 필요할 때만 읽히지만 메시지 한 통은 그쪽 세션이 온전한 한 턴을 돌게 만든다.
    - 정당한 자리는 셋뿐이다: ⑴ 내가 그쪽 트리를 건드렸다 ⑵ 지금 깨진 것 위에서 돌고 있다
      ⑶ 그쪽 판정이 있어야 내가 못 나간다.
    - 보내지 않는다: 개선·수정 알림(→ 커밋 메시지·공용 `변경일지.md`) · 관측과 수치
      (→ `기록/세션비용.csv` 와 `cost_brief` 훅) · 「받았다·반영했다·고맙다」 회신.
    - 한 사안에 왕복 1회, 한 통에 다 담는다. 세 번째 왕복이 필요해 보이면 한쪽이 판정을 안 한 것이다.

16. **수치에는 근거를 붙인다 — 숫자는 근거가 없어도 「결정」처럼 생긴다.**
    - 판정선은 둘 중 하나다: ⑴ 잰 값 ⑵ 고른 값(무엇과 무엇을 견줬는지). 어느 쪽도 아니면 그 수를 쓰지
      말고 먼저 잰다. 채팅에서 던지는 수도 같은 규율이다.
    - 기계는 `tools/audit_magic_numbers.py`(`close_report` 가 부른다). 래칫이라 새로 생긴 것만 막는다 —
      소급 면제 목록은 빚이고, 그 상수를 다시 만지면 근거를 적고 줄을 지운다.

17. **규칙을 쓸 때 「이걸 기계가 셀 수 있나」를 먼저 묻는다 — 판단형 판정선은 안 지켜진다.**
    「값어치 있으면 말해도 된다」류는 묻는 순간 언제나 「있다」가 나온다.
    - 셀 수 있는 형식 조건으로 바꾼다 — 「도구 호출 사이에 산문을 쓰지 않는다」·「사용자 발화 하나에
      보고 하나」. 요령은 「제목으로 쓸 수 있나」다: 행위 보고는 자동으로 실패하고(「나머지를 봅니다」는
      제목이 안 된다) 기계가 센다(첫 줄이 `**` 나 `#`).
    - 재는 자를 만들고 거기서 멈추지 않는다. 항상 `exit 0` 인 진단자만 있으면 숫자가 나와도 아무 일도
      안 일어난다 → 「이 수가 얼마를 넘으면 마감이 막히나」를 같이 정한다. 정할 수 없으면 아직 판정에
      못 쓴다는 뜻이고 그 사실을 적어 둔다.
    - 「진행 중계」가 그 형태로 닫혀 있다: 상한 사용자 발화 1회당 1줄 · 첫 줄이 굵은 제목인가(하한 60%).
      재는 자 `tools/audit_session_cost.py`, 막는 자 `tools/check_narration.py`.

18. **묻는 보고는 「제안」이 먼저다 — 빈손으로 묻지 않는다.** 물음으로 끝나는 응답은 **제안:** 줄
    (고른 안 + 근거 한 줄)로 시작하고, 나머지 선택지와 안 고른 이유를 뒤에 붙인다. 막혔을 때도 같은
    형식이며, 묻느라 아무것도 안 한 채 턴을 끝내지 않는다(한 것·안 한 것을 함께 적는다).
    → 본문 공용 「규칙/물어야-할-때.md」 · 세는 자 `tools/audit_session_conduct.py`.

19. **요청한 만큼만 만든다.** 요청 범위 밖의 기능·리팩터·추상화·미래 대비 설계를 더하지 않는다.
    요청이 잘못됐다고 보이면 한 문장으로 말하되 조용히 좁히거나 넓히지 않고 요청대로 낸다.
    줄이는 대상은 산출물이지 증명이 아니다 — 게이트·회귀·경계 검증·기록의 「왜」는 그대로 둔다.
    → 본문 공용 「규칙/요청한-만큼만.md」.

20. **매 세션 실리는 글(`CLAUDE.md`·`AGENTS.md`)은 간결체로 쓰고 경위는 안 실리는 자리로 보낸다.**
    규칙 여섯: 경위 안 적기 · 한 조항 = 판정선 한 줄 · 연결 산문 빼기 · 인용은 원장에 ·
    강조는 절당 하나 · 표가 산문보다 싸다. 원장·독스트링·커밋 메시지는 대상이 아니다.
    래칫 `tools/audit_guide_size.py` — 자란 것은 사람이 판정하고, 정당하면 `--accept` 로 기준선을 옮긴다.
    → 본문 공용 「규칙/실리는-글은-짧게.md」.

21. **자기 출력을 자기 증거로 쓰지 않는다.** 판정선 셋: ⑴ 새 자의 첫 출력은 결론이 아니라 **자의 검정**
    이다(합격·불합격 대조군 없이 낸 「0건」은 «없다» 가 아니라 «못 봤다» 일 수 있다) ⑵ 「닫는다」 줄의
    증거는 **이 배치에서 다시 돌린 출력**이다 ⑶ `EXPECT` 는 요구 문서에서 복사한 값이 아니라 **다른
    자가 잰 값**이다. 「확정」을 내는 자리는 그 칸을 만든 **관측 레코드**를 가리켜야 하고, 못 가리키면
    그건 「예측」이다(분류로 찍은 도장은 관측 0칸도 확정으로 만든다).
    기계 `tools/audit_stamps.py`·`tools/audit_scope_claims.py`·`tools/audit_gates.py`.
    → 본문 공용 「규칙/증거의-정직.md」.

22. **중간에 쌓인 자료는 남은 범위를 좁히는 자다 — 다만 재서 줄인다.** 단계가 바뀔 때·비싼 축이 절반을
    넘었을 때 묻는다: 「지금 손에 있는 것으로 남은 일의 범위를 좁힐 수 있나?」
    판정선: 판정 근거가 **이미 손에 있는 비싼 축**이면 줄이고, **값싼 축뿐**이면 그 자료에 규칙을 돌려
    혼동표를 채운 뒤에 정한다 — 「잃음」 칸은 나중에 안 보이므로 문턱을 그쪽에 세게 잡는다.
    안 재고 줄이면 손실이고, 안 재고 다 하면 낭비다.
    → 본문 공용 「규칙/이미-가진-것으로-줄인다.md」.

23. **키워드·상시 트리거는 이 문서에 있어야 발동한다 — grep-온디맨드로는 안 된다**(2026-09-03, 나루
    교차세션 지적으로 정정). 외부 신호(사용자가 친 짧은 말) 하나로 발동하는 규칙은 미리 알고 있어야
    그 순간 반응하지, 몰랐으면 애초에 찾아볼 계기가 없다 — 「참조형」(필요할 때 찾아본다)과
    「상시 인식형」(항상 켜져 있어야 트리거된다)은 다른 범주다. 2026-08-31 「규칙 문서 7개는
    Grep-온디맨드라 반영 불필요」 판정이 이 구분 없이 뭉뚱그려져 아래 넷을 놓쳤다.
    - **`cl`/`clear`/`클리어` 한 마디 = clear 준비 신호.** 사용자가 아무 프로젝트에서든 이렇게만
      치면 `/clear` 대신 실행이 아니라 「정리 후 짧게 보고하고 'clear 하셔도 됩니다'로 끝내라」는
      뜻이다. 절차: 채팅에만 있는 판정을 인박스·워크오더로 옮기고, 커밋·푸시하고(규율14대로 묻지
      않는다), 다음 세션이 이어받을 것을 짧게 보고한다. 정본 나루 `규칙/clear-준비.md`.
    - **일이 끝나면 남은 것을 이름으로 적고 묻는다.** 마지막 문단에 「이것으로 닫혔다」·「이거
      할까요」·「남았는지 모른다」 셋 중 하나가 반드시 있어야 한다(규율8·18과는 다른 자리 — 그건
      루프 종료 요약·질문의 형식이고 이건 **모든 완결 단위의 끝**). 정본
      나루 `규칙/남은-일은-물으며-끝낸다.md`.
    - **단정을 낮춘다.** "없다·전부·항상"으로 시작해 놓고 뒤에 "다만"을 붙이지 않는다 — 확인한
      범위만큼만 앞 문장에서 말한다. 정본 나루 `규칙/단정을-낮춘다.md`.
    - **찾았으면 고친다.** 재측정·재조사 결과를 "찾았다"로만 닫지 않는다. 고칠 수 있으면 그 자리에서
      고치고, 정당한 보류(다른 리포 소관·기능요청·판정 대기·기각됨)만 사유와 함께 열어 둔다. 정본
      나루 `규칙/찾았으면-고친다.md`.
    - 나머지 둘(`받는-순서대로-낸다`·`브라우저-다운로드-우회`)은 재확인 결과 진짜 참조형이다 —
      전자는 결과물을 건네기 직전에, 후자는 그 문서 자체가 "매 세션 자동으로 안 실린다"고 명시한
      브라우저 자동화 작업 때만 본다. 이 둘은 grep-온디맨드로 남긴다.

## ★ 공통 도구에 과목별 사실을 박지 않는다 — 폴백도 안 된다 (신설 2026-08-01)

여러 과목에서 반복해 걸린 부류다 — 도구 하나를 고치는 것으로 닫지 않는다.

- **공통 코드(`tools/**`·`site/template/**`)는 과목 이름·챕터 목록·교재 경로를 알면 안 된다.**
  그런 것은 `data/<과목>/` 의 파일이 갖는다(선례 `textbook-pdf-map.json`·`textbook-motifs.json`).
- **「폴백으로 하나만 넣어 두기」가 가장 흔한 함정이다.** 실사고(`audit_problem_originality.PDF_FOR`):
  열역학 5챕터 사전이 폴백으로 남아 ⑴ 공통이 그 과목을 계속 알고 있었고 ⑵ 다른 과목은 매핑이 없다는
  사실조차 안 드러난 채 `합계 — 정상 0` 을 찍었다. **0건이 아니라 한 건도 안 본 것**인데 출력이 통과와
  같았다. → **모든 과목이 같은 경로로만 동작하게 한다. 원조 과목도 예외를 두지 않는다.**
- **왜 반복되나 — 만드는 사람은 언제나 한 과목 안에 있다.** 판정 기준은 *"내 과목에서 도는가"* 가 아니라
  **"과목 폴더만 갈아끼우면 도는가"** 다.
- 기계 방지: 새 공통 도구를 만들 때 `test_checks.py` 에 ⑴ 소스에 과목 이름·과목별 사전이 없는지
  ⑵ 순회 대상을 `audit_content.CHAPTERS` 로 잡는지를 케이스로 넣는다. 선례
  `test_tools_do_not_hardcode_a_subject` · `test_originality_pdf_map_is_subject_local` ·
  `test_no_subject_hardcoded_data_root` · `test_shared_tests_do_not_assume_subject_data`.

## ★ 수정 배치를 반영하면 변경점 기준선도 함께 옮긴다 (신설 2026-08-01)

- **기준선은 언제나 「이번 배치 직전 상태」다.** 그래야 하이라이트가 이번에 고친 것만 가리킨다.
  사용자가 다음 검수를 '변경된 것만' 보는 방식으로 하므로, 어긋나면 검수가 못 돌아간다.
- **배치를 커밋한 뒤 곧바로 돌린다 — 요청을 기다리지 않는다**(기다리면 잊고, 잊으면 다음 검수가 엉킨다).

  ```
  python tools/build_site.py --accept-review-rev=<배치 첫 커밋의 부모 sha> --accept-review-chapter=chNN
  ```

- `changeNote` 비우기는 같은 자리에서 `python tools/clear_change_notes.py --keep=<이번 배치 표시> --apply`.
  판정선은 「이번 배치의 것인가」 하나이고 `--keep` 표시로 가른다(규칙만 있고 코드가 없어 ch05 에
  2026-07-29 자 기록이 살아남았다 — 실측 47개 중 44개가 끝난 배치 것).
- **`--accept-review-head` 는 커밋(HEAD)을 읽는다 — 작업 트리가 아니다**(2026-08-07). 커밋 전에 돌리면
  그 배치가 통째로 남는다.
  - **판정은 목록이 아니라 `review.summary` 로 한다.** 목록이 `0 / 0` 이어도 요약이 0이 아니면 화면에는
    숫자가 뜬다(실사고: ch01 요약 `수정 1` 을 목록만 보고 0으로 읽었다).
  - 순서를 못 박는다: `commit.py` → `--accept-review-head`. `git status --short` 가 빈 것을 먼저 확인한다.
- **틀리는 두 방식:** ⑴ 수정한 뒤의 HEAD → 이번 수정이 하나도 안 보인다 ⑵ 너무 오래된 지점 →
  옛 변경까지 섞인다.
- **챕터를 반드시 한정한다**(`--accept-review-chapter=`). 안 그러면 아직 안 본 챕터의 검수 상태까지 날린다.
- **검수 안 한 챕터는 기준선을 「HEAD 로 유지」한다**(정정 2026-08-07 — 「만들지 않는다」면 전부가 새
  것으로 보인다).
  - 원칙: 사용자가 아직 도달하지 않은 자리에는 변경점을 띄우지 않는다. 어차피 전부 읽어야 하기 때문이다.
  - 배치를 끝낼 때마다 이번 배치가 손댄 **모든** 미검수 챕터를 HEAD 로 민다.
  - **초기화는 배치 중간이 아니라 배치를 닫는 마지막 단계에서** 한다. 그 뒤에 또 고쳤으면 다시 민다
    (실사고 2026-08-07: 맞춘 뒤 또 고쳐 마크가 되살아났고 세 번 지적받았다).
- **사용자가 「옮기지마」라고 하면 `index.json` 의 `reviewHold` 에 적는다**(2026-08-13).
  보류를 채팅에만 두면 진다 — 다음 세션은 파일을 읽지 지난 채팅을 읽지 않는다(실사고: 인박스에 남은
  「마지막에 기준선 이동」을 실행해 수정 84항목 표시가 사라졌고 복구에 `--accept-review-rev` 6회).
  - 선언: `"reviewHold": {"at": …, "why": "<사용자 말 그대로>", "baseline": "<sha>"}`.
    있으면 `--accept-review`·`-head`·`-rev` 세 경로 전부 빌드가 거부한다(exit 1).
  - 푸는 길은 우회 플래그가 아니라 그 키를 지우는 것이다 — 지운 사실이 커밋 diff 에 남는다.
    잠금 `test_checks.py::test_review_hold_blocks_baseline_move`.
- **「다 봤어」의 기준은 시각이 아니라 커밋이다**(2026-08-13). 시각의 HEAD 로 잡으면 직전에 들어온
  커밋이 마크 없이 화면에 반영돼 그대로 사라진다.
  - 기본 명령:
    `python tools/build_site.py --all --quiet --accept-review-as-built --accept-review-chapter=chNN` —
    기준선을 **사용자가 마지막으로 본 화면의 빌드 sha** 로 잡는다(`.review-snapshot/chNN.built.json`).
  - 빌드 sha 와 밀려는 sha 사이에 그 챕터 커밋이 있으면 빌드가 목록을 찍고 거부한다. 승인은
    `--accept-review-user-saw=<sha,…>` 로 명시한다.
  - **커밋 전에는 못 민다** — 미커밋 변경이 섞인 화면은 플래그로 못 푼다(커밋 → 빌드 → 민다).
  - 판정은 `buildlib/review.unacknowledged_swallows`, 잠금 `test_baseline_move_cannot_swallow_unseen_commits`.
  - `build_review.py` 의 거부 목록은 **접두**로 본다 — 열거는 빠뜨려도 통과되고 접두는 빠뜨릴 것이 없다.
- **뷰어를 고쳤으면 `docs/viewer-change-log.txt` 에 한 줄 적는다**(2026-08-23). 변경점 판정의 입력은
  챕터 JSON 뿐이라 **뷰어·템플릿·CSS 변경은 검수 동선에서 통째로 사라진다.** 기계는 「판본이
  바뀌었나」만 판정하고(수락 기록의 `viewerRev`) 「무엇이」는 그 파일이 정본이다 —
  적힌 줄이 검수 배너로 뜬다. 잠금 `test_viewer_change_reaches_the_reviewer`.
- **변경점 판정은 이진이다 — 독자가 화면에서 알아볼 수 있는 변화만 센다**(2026-08-13).
  - 중간 등급을 만들지 않는다. 「표기만」은 판정을 미루는 이름이었고 요약 25 vs 화면 4 로 무너졌다.
    볼 것이 아니면 빌드가 표시를 아예 안 만들고, 요약·목록·화면이 그 하나의 판정만 읽는다.
  - 삽화 판정선: 글자·크기·색이 다르면 센다 / 좌표만 다르고 최대 이동 ≤ 10 화면 실효 px 면 안 센다 /
    그보다 크면 형상 변경이라 센다.
  - 저자 전용 필드(`changeNote`·`sourceRef`·`rationale`)만 바뀐 것은 변경이 아니다 — 안 빼면 사유를 지운
    자국이 다음 배치의 변경점이 된다.
  - 모르는 필드·속성은 **세는 쪽이 기본값**이다. 화이트리스트로 짜면 새 내용 필드가 조용히 사라진다.
  - 마크를 붙이는 자리와 사유를 붙이는 자리는 한 함수여야 한다(`reviewNote()` 호출부가 넷뿐이라 이론
    절·유도 카드·문풀이 화면에 안 떴고 같은 지적이 네 번 반복됐다).
  - 판정을 옮길 때는 그 판정을 잠그던 회귀 조항도 함께 옮긴다(`review-hide-drill` CSS 실례 — 13곳이
    '목록엔 있는데 안 칠해진' 상태였다).
  - 잠금 `test_review_change_is_binary` · `test_review_drill_fold_label`.

## ★ close의 정의 — 회귀 테스트가 없으면 고친 게 아니다 (신설 2026-07-22)

- **데이터를 고치는 것은 그 인스턴스를 지우는 것이지 close 가 아니다.** 같은 결함이 내일 다시 들어오는
  것을 아무것도 막지 못한다.
- **close = `tools/test_checks.py` 에 ⑴ 결함을 재현하는 케이스와 ⑵ 고친 형태가 통과하는 케이스가 들어간
  상태.** 케이스에는 `열린 날짜 / 무엇이 새어나갔는지` 를 적는다. `tools/verify_workorder.py` 가 이
  테스트를 돌린다(항목 6) — 실패하면 완료를 주장할 수 없다.
- **세션을 닫기 전에는 `python tools/close_report.py`** — 빌드·회귀·`PENDING_FIG_FIXES` 잔량·strict 승격
  잔여·미커밋을 실제로 돌려 close/미완 표를 낸다. 기억으로 "통과"라고 쓰지 않는다. 도구가 못 보는
  영역(뷰어 JS·CSS 동작)은 따로 명시한다. → 원장 2026-07-22 「고친 게 아니라 인스턴스만 지웠다」.
- **새로 만드는 검사는 「전 과목 기본 error」다.** `is_strict_chapter` 의 기본값이 error 이고, 안 켤 과목은
  그 `index.json` 의 `strictWaivers` 에 **사유와 함께** 적는다(사유 없으면 면제 아님).
  옛 키 36개는 그대로 opt-in — `docs/검사-기본값-기준선.txt` 가 그 목록이고 **늘리지 않는다**.
  기준선 파일을 못 읽으면 엄한 쪽으로 넘어진다. 잠금 `test_new_checks_are_strict_everywhere_by_default`.
- **opt-in 승격도 close 를 막는다 — 「선언 안 함」과 「보류」를 가른다.** 과목별 승격
  (`index.json` 의 `strictChapters`)은 선언해야 켜지므로 안 적으면 경고 없이 꺼진 채 남는다.
  `close_report` 가 키마다 **선언됨 / 부분 / 미선언**으로 찍고, **게이트형인데 미선언이고 보류도 사유도
  없는 것만** close 를 막는다(전부 막으면 경보 피로로 검사기가 죽는다). 보류는 `pendingChapters`,
  안 켜는 이유는 `strictWaivers` 에 사유와 함께.
- **보고 규율:** "고쳤다"고 쓸 때 **데이터를 고친 것인지 검사를 고친 것인지** 구별해 적는다. 검사를
  고쳤으면 테스트 케이스 이름을, 데이터만 고쳤으면 **"인스턴스만 수정(재발 가능)"** 이라고 명시한다.

### ★ 방지장치의 트리거는 「내가 반드시 하는 일」에 건다 (신설 2026-08-07)

「작업이 끝나는 순간」은 관측 가능한 사건이 아니라 *"끝나면 ○○한다"* 형태의 규칙은 통째로 건너뛰어진다.
반드시 하는 일은 셋뿐이다 — **턴마다 보고를 쓴다 · 커밋한다 · 빌드를 돌린다.**

- 닫힌 예: 종료 타이머를 "루프 회차 끝"이 아니라 **Stop 훅**(턴마다)으로 옮겼다.
- 안 닫혔던 예: 「배치 끝에 기준선을 민다」는 배치의 끝이 관측 불가라 하루 세 번 지적받았고,
  「배치를 **닫는 마지막 단계**」로 옮겨 닫혔다.
- 판정: 방지장치를 설계했으면 *"이게 발동하는 사건을 내가 못 보고 지나갈 수 있나"* 를 묻는다.
  있으면 성실성에 기댄 것이다(규칙 7⑷ 위반).

### ★ 도구 등록부 — 등록 안 된 도구는 없는 것과 같다 (신설 2026-08-07)

다음 세션은 `AGENTS.md` 를 읽지 `tools/` 를 훑지 않는다. 빠뜨리면 이미 있는 도구를 안 읽고 새로 만든다.
감사는 `python tools/audit_orphan_checks.py`(`close_report` 가 부른다) — ⑴ buildlib 검사 중 호출자 없는 것
⑵ `tools/` 중 어디에도 안 적힌 것. 예외는 `docs/orphan-checks-allow.txt` 에 사유와 함께.

**한 도구 = 한 줄이다 — 이름 · 용도 · 부르는 형태 · 「사람이 판정하는 자리」.** 왜 열렸나·실측·잠금
테스트 이름·설계 대안은 그 도구의 **독스트링**이 정본이다(고칠 때 필요한 것이지 부를 때 필요한 게 아니다).

- `lint_chapter.py data/<과목>/chNN.json` — 챕터 **하나만** data lint를 돈다(신설 2026-09-03).
  `build_site.py`는 과목 전체를 순회해 **다른 챕터의 기존 결함**에도 통째로 막힌다 — 지금 만지는
  챕터가 깨끗한지조차 못 본다. `checks_content.lint_chapter`를 그대로 불러 판정 기준은 같다
  · 다른 챕터를 우회하지 않는다(안 본다) · 전체 빌드·삽화 렌더는 여전히 `build_site.py` 몫
- `audit_refs.py` — 절·삽화 id 참조 감사. `[깨짐]` 0이어야 한다 · id 를 바꾸기 전에 돌린다
- `audit_conventions.py --class=P --chapter=chNN` — 표기 관행. 틀린 깃발은 조용히 무시된다 ·
  [L][P] 판정은 `card-overlap-verdicts.json` 에 사람이 적는다
- `audit_checks_load.py --lengths` — 이해도 답 문장 수 분포. 상한을 정하기 전에 재는 자리
- `audit_check_erosion.py` — 회귀가 조용히 줄었나. 정당한 삭제는 `--accept` · 판정 수를 늘리는 목표로
  쓰지 않는다(변화 감지 지문)
- `audit_orphan_checks.py` · `audit_magic_numbers.py`(상수 근거 래칫) · `audit_guide_size.py`(지침 총량
  래칫, `--accept`) — 셋 다 `close_report` 가 부른다
- `audit_chapter_completeness.py [--fail-only]` — `status: "done"`인 챕터인데 `practice`·
  `problems`가 비어 있고 `index.json`의 `contentWaivers`에 사유와 함께 선언도 안 된 경우를 잡는다
  (열린 날 2026-09-02, medesign — 이론만 채우고 done을 찍어 "남은 거 없다"고 오보한 재발 방지).
  `close_report`가 부르고 실패하면 blocker다 · 채울지 `contentWaivers`로 미룰지는 사람이 정한다 ·
  과목 이름을 하드코딩하지 않는다(`audit_content.discover_data_dir()` 재사용)
- `audit_path_names.py` — 이름이 다른 OS 로 건너갈 때 깨지나(대소문자·NFC/NFD·예약 이름).
  공개·배포 전에 `scan_private.py` 와 짝으로 · 자동으로 안 고친다
- `scan_private.py site` — 배포 표면 개인정보 · `scan_private.py --tracked` — 저장소 공유 전 Git 추적
  파일 검사(절대경로·소속·테스트 시료·한국어 합성어 오탐을 분리) · 예외는 공용
  `기록/개인정보-예외.txt` 에 사유와 함께 · 둘은 분모와 사용 시점이 달라 합치지 않는다
- `audit_tool_parallelism.py` — 도구 호출 묶음률·게이트 반복. 목표 숫자로 쓰지 않는다(추세)
- `audit_serial_waits.py [--sessions=N] [--min=60] [--project=]` — 앞에서 **기다린** 분 단위 도구 호출을
  센다(묶음률이 못 보는 축). 목표 숫자로 쓰지 않는다 · 「독립이었나」는 사람이 명령을 보고 판정한다
- `runtime_note.py -- <명령…>` · `--list` — 긴 명령의 실행 시간을 재고 `docs/실행시간.csv` 에 남겨
  **다음번에 미리 말한다**. 판정·차단은 안 한다 · 기록은 손으로 안 고친다(생성물)
- `audit_stamps.py [--selftest] [--quiet]` — 근거 없는 「확정」 도장(관측 레코드를 못 가리키는 칸).
  선언은 `docs/도장-감사.txt` 가 정본이고 **없으면 exit 1**(대상이 없으면 `없음 | <사유>` 로 적어 닫는다) ·
  1칸이라도 있으면 exit 1 · 근거가 **옳은지**는 사람이 본다
- `audit_scope_claims.py [--selftest] [--quiet]` — 「전수·전부」라 써 놓고 그 응답에서 아무 명령도 안 부른
  범위 주장. 1건이라도 있으면 exit 1 · 그 명령이 정말 그 범위를 냈는지는 사람이 본다
- `audit_gates.py [--selftest] [--run-selftests]` — 게이트의 **자·문·부르는 자** 셋이 다 있나.
  선언은 `docs/게이트-실체.txt` 가 정본 · 「자 없음」 줄을 언제 채울지는 사람이 정한다
- `close_gates.py [--selftest] [--quiet]` — 선언된 게이트를 한 번에 돌려 하나라도 실패하면 막는다.
  이 리포의 마감 정본은 `close_report` 다 · 선언에 없는 것은 안 본다
- `audit_session_cost.py --sessions=N|--all|--record` — 효율과 감수를 나란히. 한 숫자로 합치지 않고
  프로젝트끼리 비교하지 않는다. 게이트 목록은 리포 루트 `게이트-목록.txt` 가 선언한다
- `audit_session_conduct.py` — 예고하고 안 한 것 · `미검증` 방치 · 원장이 안 움직인 세션을 **센다**.
  `commit.py` 가 `--quiet` 로 부른다 · 판정·차단은 안 한다(exit 0)
- `audit_dropped_input.py [--status] [--all]` — 지난 세션에서 **답 못 받고 끝난 사용자 발화**.
  SessionStart 훅이 부르고 평소엔 침묵 · 무엇인지는 사람이 판정한다(이 자는 «답이 없었다» 만 본다) ·
  침묵이 「없다」인지 「못 읽었다」인지는 `--status`
- `audit_subject_progress.py --cards --only=` — 과목별 진도 한 표. **`git show` 로만 읽는다** ·
  판정하지 않는다
- `audit_convention_drift.py --check= --only=` — **규칙이 바뀌기 전에 쓴 콘텐츠**가 안 고쳐졌나,
  전 과목을 훑어 후보만 낸다(2026-08-29, 응용고체역학이 슬라이드모드 8/18~8/29 구멍의 첫 사례).
  체크 넷 — `slide-mode`(유도 카드에 슬라이드 figure 없음) · `fig-width`(viewBox 폭 640 아님) ·
  `hand-arc`(각도 호가 `svg_arc_arrow.py`를 거쳤는가 — 기하 정확성이 아니라 습관을 잰다,
  정확성은 `checks_svg.arc_arrowhead_issues`가 매 빌드 때 이미 잠근다) · `rate-dot`(산문에
  완성형 닷 글자가 날것으로 남았는가 — `fix_rate_dot.convert()` 재사용, 판정 불필요·바로 적용) —
  함수 하나가 검사 하나라 늘어난다 · `git show` 로만 읽는다 · 판정하지 않는다(순수 대수 유도는
  그림이 없어도 되고, 640 아닌 폭이 의도적일 수도 있다)
- `audit_fix_tool_coverage.py` — **`fix_*.py` 자체가 빌드 lint 도 `audit_convention_drift.py`
  등록도 없이 떠 있는가**(2026-08-29, "검사가 3개면 더 있을 수 있잖아, 새로 생길 때마다
  확인하는 구조냐"는 질문에서 신설). 규칙 7 「⑵ 공통 도구를 만드는 동시에 ⑶ 드리프트
  체크도 만든다」를 **글로만 적어 뒀던 것의 기계 확인판** — 첫 실행에서 7개 미확인
  (`fix_caption_block_gap`·`fix_dimension_lines`·`fix_figure_label_gap`·
  `fix_figure_vertical_balance`·`fix_given_chip`·`fix_notation_split`·`fix_polygon_to_path`)
  이 나왔다 · 소스 파일을 직접 읽는다(과목 데이터가 아니라 도구 등록부 자체를 잰다) ·
  새 `fix_*.py`를 만들면 이 도구부터 돌려 셋 중 하나(lint 신설·드리프트 등록·EXEMPT)를 한다
- `audit_fix_backlog.py [--only=갈래,…]` — 공통 `fix_*.py`를 신설·변경한 뒤 다른 과목의 기존
  콘텐츠에도 후보가 남았는지 보는 수동 진단 · 후보만 세며 판정·`--apply`는 각 과목 세션에서 한다
- `verify_all.py --tests-only --only=` — 공통을 고쳤을 때 전 갈래가 아직 서는가. 각 갈래의 자기 사본으로
- `check_floor.py .` — 바닥 방어가 **배선**됐나 일곱 항목(`@import`·훅 등록·게이트 호출부·기록·커밋의
  「왜」·공용 연결·자기 사본의 신선도). 고치지 않는다
- `check_narration.py` — 진행 중계 상한(사용자 발화 1회당 1줄 · 첫 줄이 굵은 제목 60%).
  **Stop 훅이 매 턴 `--last-turn`** · `commit`·`close_report` 는 누적을 본다(제목 하한은 턴 판정에서 뺀다 — 표본이 튄다)
- `feedback_lookup.py "<지적 요지>"` — 피드백 원장 조회. 정본은 나루 `기록/feedback-ledger.md`(331KB)
- `adopt_ledger.py [프로젝트] [--apply]` — 공용 시스템 도입 대장(판정할 빈 행을 만든다). 판정은 기계가
  절대 안 찍는다
- `worktree_names.py [--apply] [--restart]` — 워크트리 폴더 이름 규칙(`<한글표시>-<갈래>`)의 정본이자
  이동 도구. 브랜치는 안 건드린다 · 폴더가 잠겨 있어 미리보기 서버를 내렸다 되살린다
- `fill_strict_chapters.py [--apply]` — 새 챕터를 **모든 strict 목록**에 한 번에 올린다(키 목록은
  공통 상수가 정본). 넓히기만 하고 보류·면제는 안 건드린다 · 새 챕터를 만든 배치 끝에 돌린다
- `session_subject.py <갈래> --why "<사유>" | --status | --off` — 컨테이너 루트 세션의 과목 선언
  (가드 둘이 같은 표식을 읽는다 · 선언 중에는 공통이 막힌다)
- `replace_doc_section.py --file --heading --from [--apply]` — 문서의 절 하나를 갈아끼운다.
  무엇을 지울지는 판정하지 않는다
- `set_change_notes.py --chapter --from --apply` — 변경점 사유 기입(`--list` 로 현황). 사유 문장은 사람이
  쓴다 · 머리 `지적(날짜, 부류 «…»)` 는 부류마다 글자 그대로 같게(뷰어 묶음 키)
- `set_derivation_kind.py --summary=<id,…>` — 유도/공식 정리 선언. 판정하지 않는다
- `clear_change_notes.py --keep=<이번 배치 표시> --apply` — 끝난 배치의 `changeNote` 비우기
- `reorder_sections.py` · `reorder_cards.py --collection --order` — 절·카드 순서만 바꾼다.
  무엇을 바꿀지는 사람이 판정한다
- `drop_card_diagrams.py <챕터> <카드id…> --note "<사유>" [--apply]` — 카드의 `diagrams` 를 줄 단위로
  걷어낸다(재덤프하면 diff 가 통째로 바뀐다). 무엇을 지울지는 사람이 인자로 준다
- `drop_exam_steps.py <챕터> <문항id>:<칸id,…> [--apply]` — 시험 모드 채점 칸을 걷어내고 남은 칸의
  `N · ` 번호를 다시 매긴다. **어느 칸이 「떠먹임」인지는 사람이 판정한다**(같은 `choice` 라도
  「어느 식을 쓰나」와 「틀린 줄을 찾으세요」는 성격이 반대다) · 칸을 전부 지우려 하면 거부한다
- `split_problems.py --from <chAA.json> --to <chBB.json> <옛id>[=<새id>] … [--apply]` — 문항을
  다른 챕터로 통째로 옮긴다(줄 단위라 diff 가 안 뒤집힌다). `=<새id>` 는 **블록 안의 옛 id 문자열을
  전부** 바꿔 삽화 id 까지 따라오게 한다 · 무엇을 옮길지는 사람이 준다 · 원본을 비우려 하면 거부한다 ·
  옮긴 뒤 `index.json`·독자성 판정 키·검산 케이스 id 는 사람이 고친다
- `renumber_chapter.py --from chNN --to chMM [--apply]` — 챕터 번호를 갈아 끼운다(파일 이름 ·
  `chapterNumber` · 문항·삽화 id · `index.json` 의 chapters 와 **모든 strict 목록** · 교재 지도 키 ·
  검수 스냅샷 · 다른 장의 내부 링크 · 독자성 판정 키). 목적지가 있으면 거부한다 — **맞바꾸려면
  임시 번호를 거쳐 세 번** · 산문 속 「NN장」과 검산기 케이스 이름은 **사람이** 고친다
- `recolor_figures.py --map "#전=#후" [--apply]` — 삽화 팔레트 색 교체. 팔레트 값을 도구에 적지 않는다
- `render_figure_review.py "data/<과목>/chNN.json" --id <삽화id> --engine auto` — 삽화를 PNG 로
  떠 눈으로 본다(`--changed-since <sha>` 는 바뀐 것 전부). 챕터는 **위치 인자**이고
  `--chapter`·`--out` 은 **없다** · `--engine` 은 `auto|fitz` 둘뿐(`browser` 값은 없다 — auto 가
  브라우저를 잡고 `[렌더 엔진] browser` 를 찍는다) · 산출물 `review-artifacts/figure-review/chNN/`
- **삽화 조각 생성기 셋** — `svg_fraction.py`(`--line --expr`) · `svg_dimension.py`
  (`--axis h|v --p1 --p2 --face --line --fs --vb --label`) · `svg_arc_arrow.py`
  (`--cx --cy --r --from --to --vb`). 규격값은 **인자로 받지 않는다**. 잠금
  `test_svg_generators_pass_their_own_rulers`
- `svg_exp_curve.py` — 지수 감쇠·상승 곡선의 좌표를 낸다(RC 과도응답 따위). 63%·접선 작도가 **같은 τ 를
  가리키게** 계산해 준다 · 무엇을 어디에 놓을지는 사람이 정한다
- `fix_caption_block_gap.py [--chapter] [--show] [--apply]` — 그림↔캡션 블록 간격을 규격(1.5em)으로.
  캡션만 내리고 viewBox 를 같은 만큼 늘린다(상하 균형이 안 깨진다) · 무엇이 캡션인지는 빌드와 같은 함수
- `fix_miter_join.py [--chapter] [--apply]` — 예각 꼭짓점의 마이터 가시를 둥근 이음으로 전수 교정.
  좌표는 안 건드린다 · 뾰족한 것이 뜻인 도형은 고치지 말고 `lintWaivers` 로 사유와 함께 끈다
- `fix_dim_label_gap.py` · `fix_dim_extension.py` — **이미 있는** 삽화의 치수 규격 전수 교정
  (새로 그리는 것은 위 생성기). 이미 붙어 있는 것만(≤1.5em) · 80px 초과는 사람 몫
- `fix_rate_dot.py` · `fix_math_slash_fraction.py` — 자리가 다르지 기준은 하나다 · 매크로가 걸린 자리는
  `[사람]` 으로 낸다
- `fix_symbol_product.py` · `fix_numeric_product.py` · `fix_latex_brace_args.py` · `fix_honorific.py` ·
  `fix_velocity_symbol.py` · `fix_figure_caption_tier.py` — 표기 규약의 처방들. 기계가 못 가르는 것은
  `[사람]` 으로 보여주기만 한다
- `font_subset.py corpus|build --src --as` — 화면에 실제로 나오는 글자만 남긴 웹폰트. 글자 목록을 손으로
  적지 않는다 · 부분집합은 OFL 「수정본」이라 새 이름을 nameID 에 박는다
- `refresh_fonts.py --out <스크래치패드>` — 위 넷을 **한 명령으로** 다시 만든다(원본을 받아 build 를
  네 번 부른다). 빌드가 「없는 글자」를 세면 낱말을 바꾸지 말고 이것을 돌린다 · 원본은 리포에 안 둔다
- `split_textbook_pdf.py outline|split|copyout` — 교재 PDF를 책갈피 쪽 번호 기준으로 장별 분할해
  태블릿 필기용으로 만든다(2026-08-31). `outline`은 읽기 전용, `split` 산출물은
  `review-artifacts/`(커밋 안 됨, 원문이라 절대 규칙 3), `copyout`은 사용자가 그 자리에서 직접 지시한
  리포 밖 경로로만 복사한다(규칙 9 예외, 없는 폴더는 새로 안 만든다) · `--pdf` 검색은
  `extract_textbook.find_pdf` 재사용
- `verify_thermo_answer.py`·`verify_ode_answer.py`·`verify_materials_answer.py`·`verify_solids_answer.py`·
  `verify_dynamics_answer.py`·`verify_appthermo_answer.py`·`verify_appsolids_answer.py`·
  `verify_ee_answer.py`·`verify_fluids_answer.py`·`verify_mfg_answer.py`·`verify_math2_answer.py` —
  수치 독립 검산(절대 규칙 4). `index.json` 의 `answerVerifier` 로 선언해야 `close_report` 가 실제로
  돌린다 · 새 과목은 **여기와 자기 `index.json` 두 곳에** 등록한다 · 케이스가 많으면 `--fail-only`
  · **열한 과목 전부 갖췄다**(2026-08-25). 남은 것은 `family` 뿐이고 그건 콘텐츠가 없다

**감사(`audit_*`) ↔ 빌드 검사 대응** — 어느 감사가 승격됐고 어느 것이 사람 몫인지 가리는 표다.

| 감사 도구 | 빌드가 같은 것을 막나 | 남는 몫(사람) |
|---|---|---|
| `audit_orphan_checks` | — | `close_report` 가 자동 실행 |
| `audit_refs` | **예** — `xlink_target_valid`·`xlink_landing_issue`·`xlink_topic_issue` | id 를 바꿀 때 영향 범위 |
| `audit_problem_originality` | **예** — `originality_issues`(12단어 연속 일치 = error) | 판정 기록 대상 고르기 |
| `audit_problem_numbers` | **부분** — 확실한 어긋남은 **C37** | `[참고]` 층 |
| `audit_conventions` | **아니오**(K절만 C21 승격) | B·J·L·N·P 절 전부 |
| `audit_checks_load --lengths` | **예** — **C49** `check_answer_sentences` | 상한을 다시 정할지 |
| `audit_figure_balance` | **아니오** — 빌드 F1 은 하한만 본다 | 세로 균형·간격 분포 |
| `audit_symbol_intro` | **아니오** — 빌드에 그 검사가 없다 | 전부 |
| `audit_checks_load` | **아니오** — 빌드는 상한만 본다 | 절별 분포 |
| `audit_content` | **아니오**(설계상) | 사각지대 보고 |

**판정: 「아니오」를 `close_report` 로 옮기지 않는다.** 전부 사람의 판정이 필요한 성격이라 자동화하면
경보 피로가 되고, 경보 피로는 검사기를 죽인다. 표의 목적은 자동화가 아니라 "이건 이미 막히나?"에
소스 대신 여기서 답하는 것이다.

## 워크오더 수용 게이트 — `tools/verify_workorder.py`

"완료했다"고 쓰기 **전에** 스스로 돌린다. **exit 0 이 아니면 완료라고 쓰지 않는다.**

```
python tools/verify_workorder.py data/<과목>/<작업명>.workorder.md
```

검사: ① 모든 항목에 `판정:` ② **기록한 변경 파일이 실제로 바뀌었는가** ③ 미신고 변경 ④ 노랑 판단이
있으면 `## 자율 판단 기록` 존재 ⑤ 빌드 통과 ⑥ 회귀 통과 ⑦ **`## 완료 기준` 의 명령을 실제로 돌려
exit 0 인가**(자기 호출과 ⑤·⑥이 이미 돌린 것은 건너뛴다).

- **②가 핵심이다** — "수정 완료"라고 적었는데 파일은 그대로인 사고가 최소 3회 났다.
  **진행 기록은 증거가 아니다. git diff 가 증거다.**
- ⑦은 2026-08-07 신설. 형식은 *"완료 기준은 산문이 아니라 exit 0 이어야 하는 명령"* 을 요구해 왔는데
  **게이트가 그 절을 읽지도 않았다** — 수용 게이트가 자기가 요구한 것을 검사하지 않은 자리다.
  절이 없거나 실행 가능한 명령이 하나도 없으면 실패다(경고로 열어 두면 더미에 묻힌다).
- **`> 상태: 종결` 은 게이트에 아무 힘이 없다** — 한때 ⑦을 건너뛰게 했다가 같은 날 되돌렸다(완료 직전에
  한 줄 적으면 게이트가 꺼지는 길이었다). 사람이 읽는 표시로는 써도 된다.
  잠금 `test_completion_criteria_are_executed`.
- **끝난 워크오더는 다시 돌리지 않는다**(판정 2026-08-08). 이 게이트는 `기준 커밋 → 지금` 을 비교하는데
  위쪽 끝이 「지금」이라 창이 안 닫힌다 — 끝난 뒤 들어온 남의 배치가 계속 쌓인다(실측: 옛 워크오더 하나를
  지금 돌리면 기준 이후 변경 163건 중 거의 전부가 다른 챕터). **봉인 장치는 짓지 않는다** — 아무도 옛
  워크오더를 안 돌리기 때문이다(`close_report` 도 빌드도 *변경된* 워크오더만 본다). 상시 보증은
  빌드·회귀·`close_report` 가 하고, 워크오더는 **완료를 한 번 수락하는 게이트**다.
  되짚을 이유가 생기면 `docs/proposal-workorder-seal.md` 를 꺼낸다.

워크오더에는 **작업 시작 시점의 커밋**을 적어 둔다(없으면 `HEAD~1` 기준이라 부정확해진다):

```md
## 기준 커밋
<작업 시작 시점 sha>
```

## 삽화(SVG) 표준 — 정본은 `docs/삽화-규격.md` (안 싣는다)

> **삽화를 그리기 전에 `docs/삽화-규격.md` 를 읽는다.** 273줄이라 매 세션 싣지 않는다 —
> 필요한 절만 `Grep` 으로 꺼낸다(예: `rg "라벨의 기준 위치" -A 30 docs/삽화-규격.md`).
> **유도 카드(`kind:"derivation"`)의 삽화는 슬라이드 모드로 만든다** — 정본
> `docs/2026-08-18-슬라이드모드-튜토리얼-사양.md`(그리기 전에 읽는다). 열역학 ch07 이 7/7 로 실사용
> 마쳤다(2026-08-18). 이 포인터가 없어 다른 과목에 안 퍼졌던 적이 있다(2026-08-29 사용자 지적) —
> 승격했으면 여기서 가리켜야 다음 세션이 찾는다(규칙 7).
> ★ **셋 다 실사용을 마쳤다 (2026-08-25)** — 치수선은 기계재료 `fig-q04-bcc-planes`,
> 각도 호는 열역학 `fig-ch06-q07`, 분수는 그 전부터. **렌더까지 눈으로 봤다.**
> ★★ 그 첫 실사용이 **생성기가 자기 산출물로 빌드를 못 통과하던 결함 둘**을 드러냈고 닫혔다
> (`98fa1a80` — 화살촉 테두리 · 가로 치수 라벨 간격). 잠금이 **치수 규격만 재고 그 두 검사를
> 안 봐서** 초록이었던 자리다.

- **기계가 막는다(안 실어도 안 뚫린다):** 수치 규격 — 치수선 한 벌 · 각도 호+접선 화살촉 · 분수는
  `tools/svg_dimension.py` · `svg_arc_arrow.py` · `svg_fraction.py` 가 찍고, 간격·넘김·라벨 여백·
  화살촉 비율·접선은 `buildlib/checks_svg.py` 상수가 정본이다. 잠금
  `test_svg_generators_pass_their_own_rulers`. 사양 없는 삽화는 `verify_workorder.py` 3-D 가 막는다.
- **기계가 안 막는다 — 그릴 때 사람이 지킨다(그 절을 반드시 읽는다):**
  - **사양서에 「교재의 어느 쪽을 열었는지」를 적는다.** 2026-08-26 실측: 여섯 과목 사양서
    전부가 「교재와 안 겹친다」고 적었는데 근거가 틀렸고, 전부 교재를 안 열고 쓴 것이었다.
  - **색 배정**과 **글자 크기 위계**(제목 > 라벨 > 설명 캡션) — 코드로 안 내려갔다.
  - **시각 문법**: 한 시각 요소는 한 물리량만 맡는다.
  - **렌더 육안 검수는 반드시 한다** — 소스만 보고 통과시키지 않는다.
  - **"브라우저가 막혔다"는 미검수 사유가 아니다** — 못 본 것은 못 봤다고 적는다.
- 손으로 좌표를 잡지 않는다. 사람이 정하는 것은 **무엇을 어디에 놓을지**뿐이다.

## 과목 병렬 작업 — 과목 = git 브랜치 + worktree (2026-07-25 채택)

여러 과목을 각각 다른 채팅에서 동시에 작업한다.

- **과목 = 브랜치**(`thermo`·`math`…). 커밋·롤백이 과목마다 독립이다.
- **worktree 로 폴더가 갈린다.** 각 세션은 자기 폴더의 working tree 만 본다.
- **폴더 이름은 `<한글표시>-<갈래>`**(`열역학-thermo`) — 앞은 사람이, 뒤는 기계가 읽는다
  (갈래 = 마지막 `-` 뒤 ASCII). 브랜치 이름은 갈래 그대로다. 폴더 이름을 `join(부모, 갈래)` 로
  **짐작하지 않는다** — 정본 `tools/worktree_names.py`.
- **한 세션 = 한 과목.** 세션 시작에 사용자가 과목을 밝힌다. **쓰기는 자기 과목만.**
- **커밋은 자기 과목 경로만.** `git add -A` 금지 — `data/<내과목>` 만 명시적으로 add 한다.

### ★ 다른 과목은 읽어도 된다 — 단 커밋으로 읽는다 (개정 2026-08-04, 사용자 지시)

옛 규칙은 읽기까지 금지했는데 근거가 없었다. 과목끼리 배울 것이 많고(원장 승격 사례 대부분이 다른
과목에서 난 부류다), 금지 탓에 참고할 때마다 허가를 받아야 했다.

- **읽기 수단은 `git show <브랜치>:<경로>` 하나다**(예: `git show math:data/공학수학/ch01.json`).
  브랜치가 한 리포에 있으므로 어느 워크트리에서든 읽힌다.
- **남의 워크트리 폴더를 파일시스템으로 열지 않는다** — 그 과목 세션이 작업 중일 수 있어 **반쯤 고친
  상태를 정본으로 오인**한다. `git show` 는 커밋된 상태만 보여줘 그 위험이 구조적으로 사라진다.
- **인용할 때는 출처를 밝힌다** — `<브랜치>:<경로>` 와 필요하면 sha(규칙 11의 연장).
- **쓰기 금지는 그대로다** — `guard_bash.foreign_subject_paths` 가 계속 막는다. 이 개정으로 느슨해지는
  것은 없다(원래 쓰기만 기계로 막혔고 읽기 금지는 문서상 규범이었다).
- **공통 파일은 남의 브랜치에서 읽지 않는다** — `tools/`·`site/template/`·`docs/` 는 main 이 정본이다.
- **남의 워크트리의 공통 문서를 인용하지 않는다 — 낡았다.** 인용은 main 에서만 한다.
- **과목 선언은 그 워크트리의 「도는 공통 코드」가 낡았으면 막힌다** — 먼저 `git -C <워크트리> merge main`.

**기계적 강제:** `guard_bash.py` 가 현재 브랜치와 경로를 대조해 `data/<다른과목>`·`site/<다른과목>` 을
건드리는 `add`·`commit` 을 deny 한다(`foreign_subject_paths`, 회귀 `test_guard_rules`). 과목이 맞는
`add` 는 훅이 자동 허용하므로 `git add` 는 settings 의 allow 에 중복 등록하지 않는다. blocking 훅 deny가
permission allow보다 우선하며, 경계 판정은 `deny_reason()` 한 곳이 맡는다.

## ★ 챕터 병렬 작업 — 챕터 = `<과목>-chNN` 브랜치 + worktree (신설 2026-07-29)

챕터 콘텐츠는 `data/<과목>/chNN.json` 으로 완전히 갈려 있고 산출물은 `.gitignore` 라 추적되지 않는다.
충돌 지점은 **공통 하나뿐**이고 그건 소유자를 못 박아 막는다.

| 층 | 브랜치 | 커밋 가능 범위 |
|---|---|---|
| **과목 본 세션** | `thermo` | **공통 전부** + 자기 과목 데이터 |
| **챕터 세션** | `thermo-ch02` … | **`data/<과목>/chNN*` 만** |

- **공통(`tools/`·`AGENTS.md`·`CLAUDE.md`·`site/template/`·`.claude/`)은 과목 본 세션 하나가 소유한다.**
  챕터 세션이 커밋하려 하면 `guard_bash.out_of_chapter_paths` 가 deny 한다. 근거: 2026-07-27 클로버
  사고의 원인은 부주의가 아니라 **뒤진 브랜치가 공통을 덮을 수 있는 구조**였고, 병렬 세션을 늘리면 그
  확률만 커진다.
- **규격을 고쳐야 하면** 챕터 세션이 직접 고치지 말고 본 세션에 요청한다. 본 세션이 커밋하면
  `git merge thermo` 로 받는다. 챕터 작업이 끝나면 본 세션에서 `git merge thermo-chNN`.
- **새 챕터 worktree:** `git worktree add ../thermo-chNN -b thermo-chNN thermo`
  (브랜치 이름은 `CHAPTER_BRANCH_RE` 가 정본).
- **`claude --worktree`(`-w`)를 이 리포에서 쓰지 않는다**(2026-08-14). 그 플래그는 `worktree-<이름>`
  브랜치를 만드는데 **우리 경계는 브랜치 이름으로 판정한다** — 이름이 안 맞으면 가드가 「과목을 모르는
  브랜치」로 빠져 **경계가 꺼진 채 돈다.** 공식 기능이라고 반사적으로 쓰지 않는다.
- 빌드는 각 워크트리에서 그대로 돌린다(산출물이 추적되지 않아 서로 안 밟는다).
- 잠금 `test_guard_rules` 의 「챕터 병렬 브랜치」 11건 — 접두어 매칭이 없으면 병렬화는 느려지는 게 아니라
  **과목 경계가 꺼진 채** 돈다(`foreign_subject_paths` 가 `mine is None` 으로 빠진다).

## ★ 콘텐츠 표기 규약 — 정본은 `docs/콘텐츠-표기-규약.md` (안 싣는다)

> **본문·유도·문풀을 쓰기 전에 `docs/콘텐츠-표기-규약.md` 를 읽는다.** 185줄이라 매 세션 싣지 않는다 —
> 필요한 절만 `Grep` 으로 꺼낸다(예: `rg "기호 사이" -A 25 docs/콘텐츠-표기-규약.md`).

- **기계가 막는다(안 실어도 안 뚫린다):** 블록 형태 판정(`checks_content.classify_block` ·
  `test_viewer_block_forms_match`) · 문항 지문 언어 **C15** · 가운데점 **C17** · 절 참조 **C22** ·
  말투 **C23** · 기호 곱 **C36** · 유도/공식 정리 **C38·C43** · 매크로 중괄호 **C39** ·
  한 글자 두 물리량 **C44** · 이해도 답 길이 **C49**. 처방 도구는 `fix_*` 계열(도구 등록부 참조).
- **기계가 안 막는다 — 쓸 때 사람이 판정한다:**
  - 넷 중 **어느 블록을 쓸지**(불릿·번호·표·디스플레이 수식). 콜아웃·접기는 쓰지 않는다.
  - **유도인가 공식 정리인가** — 판정선은 「앞에서 얻은 식을 받아 다음 식을 만드는 자리가 있는가」.
    애매하면 유도로 두고 인박스에 올려 판정을 받는다.
  - **「잘 놓쳐요」와 「떠올리기」 겹침** — 자는 후보와 점수만 낸다. 점수를 판정으로 읽으면 멀쩡한
    카드를 지운다. 판정은 `data/<과목>/card-overlap-verdicts.json` 에 근거와 함께.
  - **응용 예시는 그 과목의 학과 맥락에서** 고른다(학과 맥락은 각 과목 `.claude/SUBJECT.md`).

### ★ 받은 지적은 피드백 원장에 먼저 적는다 (정본 이전 2026-08-16)

- **정본은 나루 `~/Documents/naru/기록/feedback-ledger.md`** 다. 리포 안에만 두면 프로젝트 사이 재발을
  볼 자리가 없다. 리포의 `docs/feedback-ledger.md` 는 머리에 「정본은 나루」를 박은 채 남아 있다.
- 지적을 받으면 **고치기 전에** 한 줄 적는다 — `부류(원칙)` · `범위(공통/과목별)` · `기계 방지`.
- **읽는 법은 통독이 아니라 조회다** — `python tools/feedback_lookup.py "<지적 요지>"`(원장 331KB).
- `기계` 칸이 빈 채로 항목을 닫지 않는다(「close의 정의」).
- **데이터 인스턴스 수정**은 원장이 아니라 `data/<과목>/chNN-review-inbox.md` 에 쌓는다.
- **승격된 것만 공통이 된다** — 지적을 워크오더에만 남기면 그 교훈은 그 과목에 갇힌다.

## ★ 목표 독자 — 처음 보는 사람도 따라올 수 있게 (신설 2026-07-26 · 같은 날 정정)

기본값은 **그 과목을 처음 보는 사람**이다. "수업을 방금 들은 사람"을 가정하면 설명이 통째로 빠진다.
선수과목 기억이 흐린 독자도 같은 기준 안에 들어온다 — 별도 기준이 아니라 같은 기준의 결과다.
(이 절은 처음에 한 개인의 사정을 규칙으로 올렸다가 일반화했다 — **공통 문서에 과목·개인 사정을 넣지 않는다.**)

- **기호가 어디서 왔는지 밝힌다.** 앞 절에서 정의한 문자를 말없이 바꾸지 않는다(실사고: 비완전식을
  `P dx + Q dy` 로 선언하고 다음 문장에서 `M`·`N` 으로 갈아탔다).
- **"왜 이걸 배우나"를 절마다 한 줄 이상 준다.** 방법만 있고 쓰임이 없으면 독자는 넘긴다.
- **결과만 쓰지 말고 확인 단계를 남긴다**(교재도 적분인자를 곱한 뒤 완전성을 다시 검사하고 답을 대입 검산한다).
- 판정 기준: **"이 문단만 읽고 막히는 곳이 있나"** — 있으면 그 자리에 한 줄을 넣는다. 사용자가 의문을
  품은 지점은 처음 보는 독자도 똑같이 걸린다고 본다.
- **실제 배포 대상이 과목마다 다르면 `.claude/SUBJECT.md` 에 적는다.** 전제할 수 있는 선수 지식은
  과목별 사실이라 공통에 두지 않는다 — 공통은 "처음 보는 사람 기준"만 유지하고 완화는 과목 문서에서 한다.

## ★ 문제 설계 — 난이도 눈금과 유형 축 (신설 2026-07-26)

"적당히 쉬운 것 몇 개, 어려운 것 몇 개"로 만들면 쉬운 쪽으로 쏠린다(실측: 공학수학 ch01 9문제 중
`advanced` 0건). 그래서 눈금과 비중을 못 박는다.

**⑴ 유형 축이 난이도보다 먼저다.** 방법마다 아래 3축을 각각 최소 1문제씩 덮는다.

| 축 | 성격 | 예 |
|---|---|---|
| **A 기본형** | 방법이 주어짐. 절차 재현 | "변수분리로 일반해를 구하라" |
| **B 변형** | 표준형 만들기·치환 지정·초기조건 | "먼저 표준형으로 고칠 것", IVP |
| **C 판단** | 방법 미지정·함정·역방향 | "어느 방법인지 밝히고 풀어라" |

방법이 N개면 **문제 3N개**가 적정선이다. 축 C가 비면 독자는 방법을 고르는 능력을 못 얻는다.

**⑵ 난이도 눈금 1~10 — 상한은 7.** 교재 문제로 눈금을 박아 판정 가능하게 한다.

| 난이도 | 성격 | 교재 대응(Kreyszig 1장) |
|---|---|---|
| 1-2 | 정의·대입 검산 | 리뷰 질문 1~10 |
| 3-4 | 방법 주어진 기본 계산 | §1.3 Prob 2~5 |
| 5-6 | 표준형 변환·치환·IVP | §1.3 Prob 11~17 |
| **7** | 방법 미지정 + 함정 포함 | 1장 리뷰 17~26 · §1.7 Prob 2 |
| 8 | 역방향·조건 찾기 | §1.7 Prob 10 (챕터당 1문제까지) |
| 9-10 | 증명·프로젝트·CAS | **제외** |

9-10 제외 근거: 그 구간의 난이도는 ODE 가 아니라 **적분 기교**에서 온다 — 기르려는 축이 아닌 곳에서
난이도가 오르면 변별이 아니라 잡음이다.

**⑶ 비중 = 쉬움 3 : 보통 5 : 변별 2**(난이도 3-4 / 5-6 / 7-8). 부교재의 7:2:1은 **반복 훈련용**
(방법당 40문제)이고 우리는 **유형 지도**(방법당 3문제)다 — 반복은 부교재에서 한다.
문풀(빈칸)은 3~5, 연습문제가 5~7을 맡는다.

**⑷ 시험에 주어지는 것과 아닌 것을 갈라 배분한다 — 단, 확인된 과목에서만.** 제공되는 공식은 외울
필요가 없으니 **적용·선택(축 C)** 으로, 제공되지 않는 방법은 **축 A·B를 두껍게.** 실측 목록은 그 과목
`.claude/SUBJECT.md` 에 적는다(공통에 특정 과목의 공식 목록을 박지 않는다). 시험 자료가 이미지 PDF 면
`python tools/extract_textbook.py --pdf <조각> --pages a-b --render` 로 PNG 를 만들어 직접 읽는다 —
"텍스트가 없어 못 읽는다"는 사유가 아니다.

**⑸ 「답」과 「공식 암기」만 묻지 않는다.** 무게는 **과정과 방법 선택**에 둔다 — 축 C 가 본체다.
풀이는 단계가 보이게 쓴다. 시험 직전 부분 암기가 필요한 항목은 그것대로 표시해 준다.

**⑹ 독자 환경을 전제하지 않는다** (2026-08-01 사용자 재지적).

- 사용자가 알려준 수업 사정은 **제작 참고 정보**다. `.claude/SUBJECT.md` 에 두는 것은 정당하지만
  **독자 화면에 사실로 단정해 옮기면 안 된다** — 독자는 다른 교수·다른 학기일 수 있다.
- 본문에는 조건부로 쓴다: `이 과목은 공식집을 줍니다` ✗ → `공식집을 주는 수업이라면 …` ✓.
  실측 목록은 "확인된 한 사례"로 명시한다.
- **공통 규칙에 한 과목의 사정을 올리지 않는다** — ⑸ 가 실제로 그렇게 됐던 자리다.
- 부류: **승격에 브레이크가 없었다.** 세 층을 가른다 — **제작 참고(SUBJECT.md) / 독자에게 제시할 사실 /
  전 과목 규칙.**
- 기계: 독자 노출 필드에서 `공식집`·`우리 수업`·`이번 학기` 를 단정형으로 쓰면 빌드가 막는다
  (`checks_content.reader_environment_issues`, 잠금 `test_reader_environment_is_not_assumed`).
  조건부 표현은 통과한다.

## ★ 과목 개요와 챕터 도입부 — 큰 흐름을 먼저 준다 (신설 2026-07-26)

교재는 본론 전에 반드시 지도를 준다. Kreyszig는 Part 표지에서 *"이 파트의 목표는 둘 — ODE 풀이법과 모델링"*
이라 밝히고, 챕터 표지마다 `이 장이 하는 일 / Prerequisite / 생략 가능한 절`을 적는다
(§2 표지: *"Prerequisite: Chap. 1, in particular, Sec. 1.5"*, §1 표지: *"Sections that may be omitted: 1.6, 1.7"*).
**우리 자료에는 이게 통째로 없었다** — 챕터를 열면 곧바로 학습목표가 나온다(2026-07-26 사용자 지적).
처음 보는 독자에게 가장 먼저 필요한 것이 이 지도다.

- **과목 개요(0장)** — 과목당 하나. `이 과목이 무엇을 위한 것인가` · `장이 어떤 순서·연관으로 이어지는가` ·
  `어디까지가 수업/시험 범위인가`.
- **챕터 도입부** — 챕터당 최소 세 줄. `이 장이 답하는 질문` · `선수 지식(앞 장 어느 절)` · `다음 장과의 연결`.
- 근거는 **교재의 Part·Chapter 표지와 수업 자료의 범위 표시**에서 가져온다. 없는 연결을 지어내지 않는다(규칙 2).
- **★ 과목 개요(0장)에는 이해도 체크(「설명하기」·「떠올리기」류) 카드를 두지 않는다**(신설 2026-09-01,
  사용자 판정 — 이해도 체크는 실제로 학습이 일어나는 장에서 절을 떠올리게 하는 장치라 0장에는 안 맞는다는 취지). 이해도 체크는 **본문에서 배운 절을 스스로 인출하게 하는 장치**인데, 0장은 절 단위 학습이
  일어나지 않는 지도·범위 안내 자리라 인출할 대상 자체가 없다 — 카드를 두면 형식만 흉내 낸 빈 정원이
  된다. 이미 있는 과목의 0장은 이 규칙을 몰라서 만들어졌을 수 있으므로, **각 과목 세션에서 자기
  ch00.json 을 열 때 이 절을 확인하고 걷어낸다**(전 과목 소급, 이 문서 자체는 공통이라 여기서 한 번만
  선언한다).

**전 과목에 적용한다 — 열역학 포함**(사용자 지시 2026-07-26).

## 알려진 함정 (재발 금지)

- **`git diff --no-index` 로 리포 밖 파일과 비교하지 않는다**(2026-08-14 실측). 같은 파일 쌍을 두 번
  쟀는데 «31줄/56줄 다름» 과 «차이 없음» 이 번갈아 나왔고, 그 «차이 없음» 을 믿고 정정 보고까지 냈다 —
  `wc -cl` 로 재니 301줄 vs 276줄로 실제로 갈려 있었다. 원인 미확정(한글 경로·`core.autocrlf`·병렬 실행).
  **확정 못 한 것이 요점이다 — 이 조합은 조용히 틀린 답을 준다.**
  → 리포 밖 비교는 `wc -cl` 로 먼저 재고, 내용 판정이 필요하면 양쪽을 각각 `Grep` 으로 훑는다.
  **재는 명령을 병렬로 여러 개 돌리지 않는다**(결과가 뒤섞인 정황이 같은 세션에 두 번).
- LaTeX 문자열: 셸을 거치면 백슬래시가 깎인다 → 데이터 수정 스크립트는 **파일로 저장 후 실행**.
- JSON 주입: 정규식 replace 금지(문자열 replace 만) · `${` 금지 · 제어문자 금지 — 빌드가 검사한다.
- `hidden` 속성 + 저자 `display:` 충돌 → 템플릿의 `[hidden]{display:none!important}` 가드를 지우지 않는다.
- plain 텍스트 필드는 유니코드 위첨자(m³·m/s²), LaTeX 필드만 `^{}` — 캐럿 잔존도 빌드가 검사한다.
- 아래첨자는 **뷰어가 `fmtText` 로 렌더하는 필드에만** 쓴다. `esc()` 로 렌더되는 필드에 넣으면 화면에
  `dE_system/dt` 처럼 날것으로 나온다(빌드가 검사하고, 새 필드는 등록이 필요하다).
- **인코딩은 `sys.stdout.reconfigure(encoding="utf-8", errors="replace")`.** `io.TextIOWrapper` 로 감싸면
  `build_review.py` 가 `build_site.py` 를 runpy 로 부를 때 이중 래핑이 되어 첫 wrapper 가 GC 되며 buffer 를
  닫는다(`ValueError: I/O operation on closed file`). **`tools/*.py` 의 인코딩을 손대면 반드시
  `build_review.py --all` 까지 재검증한다** — 단독 실행으로는 안 드러난다.
- **바깥에서 들어오는 텍스트는 무조건 UTF-8 로 명시해 읽는다.** Windows 기본이 cp949 라 명시하지 않으면
  한글이 **예외도 없이 조용히** 깨진다 — 훅 페이로드는 `read_payload()`, `subprocess.run(text=True)` 에는
  `encoding="utf-8"`. ASCII 만 보는 규칙은 멀쩡해서 **가드가 도는 것처럼 보이는 것**이 이 부류의 무서운
  점이다(실제로 과목 경계가 통째로 죽어 있었다). → `guard_bash.read_payload()` 독스트링 · `test_guard_rules`.
- **나가는 쪽도 마찬가지다 — `stderr` 를 안 고치면 「오류 메시지만」 깨진다**(2026-08-12). 실측: 도구 79개가
  `sys.stdout.reconfigure` 를 부르는데 stderr 를 고친 것은 하나뿐이었다. 정상 출력은 멀쩡한데 **사람이 가장
  읽어야 할 글만** 깨진다. → `sys.exit("한글…")`·`print(…, file=sys.stderr)` 를 쓰는 도구는
  `sys.stderr.reconfigure(encoding="utf-8", errors="replace")` 도 함께 둔다. 잠금 `test_tool_failures_are_readable`.
- **「대상이 아닌 과목」은 실패가 아니다.** 과목이 선언해야 도는 도구가 선언 없는 과목에서 exit 1 을 내면
  배치와 사람 둘 다 «실패했다» 로 읽는다. 선언이 곧 opt-in 이므로 그 경우는 `[해당 없음] … 0건` 을 찍고
  **exit 0** 으로 끝낸다.

## 워크오더 — 자기 규율용 작업 기록

Codex 분업이 꺼지면서(2026-07-27) '핸드오프' 목적은 사라졌다. 하지만 워크오더의 **품질 규율**은
단독 작업에도 그대로 유효하다 — 큰 작업은 계획을 파일에 박아두고 그 계획으로 스스로 검증받는다.

- **큰 작업**(챕터 재검·삽화 배치·구조 변경)은 `data/<과목>/<작업명>.workorder.md`에
  범위·수용 기준·**항목별 `판정:`**(규칙 8)·**기준 커밋**을 적는다.
- 완료를 주장하기 전 `python tools/verify_workorder.py <문서>`가 **exit 0** 이어야 한다
  (위 '워크오더 수용 게이트'가 정본).
- 노랑 구역 판단을 했으면 `## 자율 판단 기록`을 남긴다(규칙 10) — *무엇을 / 근거 / 되돌리는 법*.
- **자잘한 단발 수정까지 워크오더로 만들 필요는 없다.** 부류 전수 감사가 붙는 규모일 때 쓴다.
- 애매하면 진행하지 말고 **사용자에게 먼저 묻는다** — 특히 콘텐츠 문구·삭제가 필요해 보일 때.
- 과거 두-에이전트 핸드오프 프로토콜·Codex 진행 기록 형식은
  `docs/archive/codex-workflow.archived.md`에 보관돼 있다(재분업 시 복원).

### 작업지시서 형식

새 workorder 맨 위에는 아래 항목을 짧게 둔다. 긴 배경 설명은 그 아래에만.

```md
## 기준 커밋          ← 작업 시작 시점 sha (verify_workorder가 이걸로 실제 변경을 대조한다)
## 실행 범위
## 수정 가능 파일
## 완료 기준          ← 산문이 아니라 **exit 0이어야 하는 명령**으로 적는다
## 금지 사항·검증 방법
## 항목별 판정        ← 항목마다 `판정: 수용/반박/조건부` + 근거 한 줄
## 자율 판단 기록      ← 노랑 구역 판단을 했다면 (무엇을/근거/되돌리는 법)
## 진행 기록
```

**완료 기준은 검증 가능해야 한다.** "삽화가 규격을 통과한다"는 누구도 판정할 수 없다.
대신 이렇게 적는다:

```md
## 완료 기준
- `python tools/verify_workorder.py data/열역학/<작업명>.workorder.md` exit 0
- `python tools/audit_content.py` 사각지대 0건
```
