# AGENTS.md — Astra/Codex 작업 어댑터

> **이 저장소를 방금 받았다면(툴킷 첫 세션).** 이 파일은 참고 자료가 아니라 **이 폴더에서 일하는 규칙**이다 —
> 그대로 채택한다. `data/` 에 과목이 없으면 `START.md` 순서대로 사용자와 첫 과목을 세운다.
> 아래 본문은 원작자 실사용본이라 21과목·배포·「공용 폴더」 이야기가 섞여 있다 — 없는 것은 건너뛴다.

이 파일은 Codex 계열 에이전트가 이 저장소에서 작업할 때의 짧은 상시 규칙이다. Claude Code의 상세 규칙 정본은 `CLAUDE.md`에 있고, 두 문서를 매번 통째로 복제하지 않는다.

## 작업 시작

- **`docs/인계.md` 맨 위 두어 줄을 먼저 읽는다.** 그 줄의 sha 이후 커밋은 `git log <sha>..HEAD --format=%s` 로 본다 — 상대 도구(Claude)가 한 일이다. 매 턴 읽는 파일이 아니라 시작할 때 한 번이다.
- 사용자가 지정한 경로·범위·기준 커밋을 먼저 고정한다. 범위를 넓히지 않는다.
- 큰 파일을 먼저 전부 읽지 말고, 파일 목록·변경점·관련 제목을 훑은 뒤 필요한 부분만 연다.
- 과목 콘텐츠를 다룰 때 해당 `data/<과목>/SUBJECT.md`와 필요한 `.claude/rules/content.md`를 먼저 읽는다. 그림·도구·뷰어는 각각 `figures.md`·`tools.md`·`viewer.md`를 필요한 때만 읽는다.
- 기본은 읽기 전용 검토다. 수정 요청이 있을 때만 수정하며, 기존 미커밋 변경을 보존한다.

## 검토와 기록

- 기존 보고서·검사기·메모리는 권위가 아니라 검토 대상이다. 먼저 독립적인 결함 후보와 재현 근거를 기록한 뒤 대조한다.
- 같은 내용을 다시 읽거나 같은 검사를 반복하지 않는다. 기준 커밋과 입력 해시가 그대로일 때만 이전 결과를 재사용한다.
- 중단·컴팩트에 대비해 현재 작업의 기준선, 확인 범위, 확정·미확정 판정, 다음 한 가지 행동을 `docs/작업-재개-기록.md`에 남긴다.
- 결함은 파일·줄·기대/실제 동작·재현 절차·영향·신뢰도를 함께 적는다. 검사기 출력만으로 결론내리지 않는다.
- **사용자에게 묻는 것은 배치 끝에 몰아서 한 번.** 도중에 생긴 판정 필요는 그 장 `chNN-review-inbox.md` 에 `☐ 번호`로 적고 다음 항목으로 계속한다. 답과 무관한 것은 먼저 해 둔다. 건마다 채팅을 보내지 않는다(사용자 2026-09-17 *[발화 생략]*). 물을 때는 빈손 금지 — `**제안:** <하나> — <근거>` + 선택지 표.

## 완료 조건

- 데이터나 템플릿을 바꿨으면 `python tools/build_site.py` 또는 작업에 맞는 검증기를 실행하고 결과를 확인한다.
- 검토·수정·검증을 구분해 보고한다. 근거 없이 "완료"라고 쓰지 않는다.
- 배포는 사용자가 명시한 경우에만 다룬다. `vercel deploy`를 자동으로 실행하거나 기다리지 않는다.
- **끝낼 때(한도가 차서 넘길 때 포함) `docs/인계.md` 맨 위에 한 줄** — 도구 · 마지막 커밋 sha · 어디까지 · 다음 · 기다리는 ☐. 미커밋을 남기지 않는다(미완이면 되돌리고 「어디까지」만 적는다). 인계 줄 없는 커밋은 다음 세션이 결함으로 센다.

세부 규칙, 출처·저작권·삽화·공통 시스템·커밋 규율은 `CLAUDE.md`와 해당 경로 규칙을 따른다. 이 어댑터와 세부 규칙이 충돌하면 사용자 요청과 더 구체적인 경로 규칙을 우선하고, 충돌을 보고한다.
