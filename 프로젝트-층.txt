# 이 프로젝트의 «층» 선언 — `tools/adopt_ledger.py` 가 읽는다 (공용 `규칙/프로젝트-층.md`).
#
# ★ 숫자만 적은 선언은 선언으로 안 친다 — 네 물음의 답이 함께 있어야 한다.
#   「간단해 보이니 0층」이라는 판단형이 그 문서가 막으려는 뒷문이기 때문이다.
#
# ★★ 이 파일은 2026-08-18 에 **뒤늦게** 만들었다. 도입 대장에 2026-08-15 자로
#   *"리포 루트에 프로젝트-층.txt 를 세웠다"* 고 적혀 있었는데 **실물이 없었다**
#   (리포 전수 grep 0건 · adopt_ledger 가 계속 「선언 파일이 없다」로 전부 물어 왔다).
#   공용이 2026-08-18 에 연 «대장의 「가져옴」이 실물과 어긋난다» 부류의 두 번째 실측이다.
#
# ★★★ 2026-08-24: main 과 solids 가 **각자** 이 파일을 만들어 add/add 충돌이 났다
#   (`sync_common --merge-all` 이 12갈래 중 solids 하나에서 걸렸다). 판정은 양쪽 다 층 2 로
#   같았고 근거는 solids 판이 더 자세해 그것을 본문으로 삼았다. 공통 파일이므로 **정본은 main**
#   이고 solids 는 이 판을 받는다 — 같은 선언을 두 갈래가 따로 적으면 다음 공통 전파가 또 막힌다.

층: 2
되돌릴 수 없는 것: 있음 — Netlify 로 학습 자료를 게시한다(`tools/deploy_prebuilt.ps1` 수동 실행 ·
  프로덕션은 15크레딧 정액). 사이트는 엣지 함수로 HTTP Basic 이 걸려 있어 공개 인터넷은 아니지만,
  **배포는 배포다** — 한 번 나간 것은 재배포로만 되돌린다. 2026-08-14 에 실제로 발행본에
  개발 주석 217줄과 학교 이름이 실려 나가 있던 것을 뒤늦게 잡았다.
남이 읽나: 예 — 자료를 읽는 사람이 있다(비밀번호를 받은 사람). 그래서 교재 대조 없는 창작을
  금지하고(no-fabrication) 답 수치는 `verify_solids_answer.py` 가 독립 재계산으로 검산한다.
  **읽는 사람이 이걸 사실로 받는다**는 것이 이 리포의 규칙 절반의 근거다.
오래 사나: 예 — 과목 12개를 학기 내내 계속 손댄다. 챕터를 몇 달 뒤 다시 열어 고치므로
  회귀 하네스(`tools/test_checks.py`)와 도구 등록부가 실제로 값을 한다.
여럿이 만지나: 예 — **워크트리 13개**가 같은 공통 파일(`tools/`·`AGENTS.md`·`site/template/`)을
  만지고, 공용 폴더(나루)와도 미러로 이어져 있다. 2026-08-14 하루에만 같은 파일을 두 세션이
  각자 고쳐 충돌이 **두 번** 났다(`strip_dev_comments` · 이 대장 자체).

# 판정: 네 물음이 **전부 「예」**라 층 2 다(§2·§3). 층은 누적이라 0·1 을 포함한다.
# ★ 「간단해서 낮은 층」은 선언이 아니다(§5) — 위 넷의 답으로만 정했다.
# ★ 답이 바뀌면 그 자리에서 다시 선언한다(§4). 이 리포에서 바뀔 만한 자리는 하나다 —
#   사이트의 Basic 인증을 풀어 공개로 돌리는 순간. 그때는 층이 아니라 ⑴·⑵의 근거가 세진다.
