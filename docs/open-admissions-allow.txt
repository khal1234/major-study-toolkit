# 코드 자백 래칫 면제 — `tools/check_open_admissions.py` 가 읽는다.
# 형식: <파일> | <줄 앞머리 60자> | <사유: 다른 리포 소관 / 기능 요청 / 사람 판단 대기 / 기각됨 — 무엇>
# 사유 칸이 빈 줄은 면제로 안 친다.
.claude/hooks/open_items.py | 거기 「열림」은 *기계 방지가 아직 없다* 는 뜻이지 *사용자가 답을 기다린다* 는 뜻이 아니다. | 기각됨 — 원장 「열림」의 뜻을 설명하는 문장이지 자백이 아니다
tools/audit_conventions.py | #   y·색·앵커를 손으로 복원해야 했다. 그 복원이 이 부류를 고치는 비용의 | 기각됨 — 과거 사고 서술(했다)이지 남은 일이 아니다
tools/buildlib/ledger.py | # 미구현이고 무엇이 표기 문제인지**를 갈라 준다 — 그 둘은 갚는 비용이 자릿수로 다르다. | 기각됨 — 원장이 가르는 분류 이름 「미구현」의 설명
tools/buildlib/ledger.py | 진짜 미구현과 갚는 비용이 자릿수로 다르므로 갈라 보는 것이 먼저다. | 기각됨 — 원장이 가르는 분류 이름 「미구현」의 설명
tools/new_subject.py | 빠진 항목은 지우지 않고 **TODO 로 남긴다** — 없는 것이 눈에 보여야 채워진다. | 기각됨 — 새 과목 뼈대에 TODO 표식을 찍는 기능 설명
tools/register_chapter.py | 완화(= 승격 해제·삭제)는 여전히 손으로 `Edit` 해야 하고, 그때는 승인창이 그대로 뜬다. | 기각됨 — 완화를 사람 손에 남긴 것은 설계(검사 완화는 빨강)
tools/test_checks.py | 못 받은 항목은 지우지 않고 TODO 로 남긴다(없는 것이 보여야 채워진다). | 기각됨 — new_subject 의 TODO 표식 기능을 시험하는 회귀 설명
