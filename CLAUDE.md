# CLAUDE.md

이 리포의 작업 규칙은 `AGENTS.md`(공통)와 `.claude/SUBJECT.md`(이 과목 전용)에 있다.
아래 import로 매 세션 자동 적용된다 — 별도로 Read 하지 않아도 된다.

@AGENTS.md
@.claude/SUBJECT.md

> **공통(`AGENTS.md`) vs 과목별(`.claude/SUBJECT.md`) 분리 (2026-07-27).** 공통은 **main이 정본**이고
> 각 과목 브랜치가 `git merge main`으로 받는다. `SUBJECT.md`는 이 브랜치에만 있다.
> 세션 시작 때 공통이 main보다 낡았으면 `.claude/hooks/common_guard.py`가 "먼저 merge하라"고 알린다.

> **2026-07-27부터 Claude 단독 운영이다** — Codex 분업은 껐다(재분업 대비 보관: `docs/archive/codex-workflow.archived.md`).
> 그래서 콘텐츠·삽화·구현을 **모두 Claude가 한다.** "이건 Codex 몫"이라며 미루지 않는다.

## 구역 선언은 착수 전에 (AGENTS.md 규칙 10)

작업을 받으면 **착수 전에** 초록/노랑/빨강을 분류해 선언한다.
사용자가 "이건 하면 안 되는 건데"를 기억해 막아주길 기대하지 말 것 — **막는 건 내 책임이다.**

구역 판정 기준은 AGENTS.md 규칙 10이 정본이다(여기 다시 쓰지 않는다).
요점만: **교재 대조를 마친 콘텐츠·삽화 작성은 초록**,
**출처 없는 창작·삭제·배포·`error_log.json` 수정·검사 완화는 빨강.**

## ★ 매 응답 끝에 프롬프트 요령 3줄 (빠뜨리지 말 것)

사용자 요청(반복 학습용). **사용자가 직접 지시한 응답의 마지막에** 아래 블록을 붙인다 —

> ★ 「/loop」 회차 보고에는 붙이지 않는다(2026-08-12) — 왜 매 회차에 붙이는지 반문한 지적.
> 이 블록은 *사용자가 프롬프트를 쓰는 자리*에서 배우게 하는 장치인데, 루프 회차는 **저장된
> 프롬프트가 자동으로 도는 것이라 받는 사람이 없다.** 규칙을 문자 그대로 적용해 회차마다
> 붙였던 것이 잘못이다. 루프가 끝나 사용자에게 돌아가는 마지막 보고에는 붙인다.

`이번에 좋았던 지시 1줄` / `이렇게 쓰면 더 좋다 1줄` / `다음에 쓸 만한 표현 1줄`.
메모리에만 두었더니 세션마다 사라져 **2회 지적**받았다(2026-07-26 시스템화).
사용자가 "당분간 그만"이라 하면 즉시 중단하고 이 절을 지운다. 안 지켰다고 사용자가 다시 지적할 필요가 없어야 한다.

## ★ 채팅 말투는 존댓말 `~입니다` (공용 「규칙/말투.md」에서 받음 2026-08-16)

- 세 층 표의 정본은 `docs/콘텐츠-표기-규약.md` 「말투」 절이고 **채팅 층만 여기 싣는다** —
  다른 둘(콘텐츠 존댓말 · 리포 문서·커밋 메시지 평어)은 **`C23`·`fix_honorific` 이 막는데
  채팅은 아무도 안 막는다.** 안 실으면 규격이 없는 것과 같다(나루 원장 2026-08-02 재발).
- **저장소 문체가 채팅으로 샌다** — 평어로 문서를 쓴 세션일수록 그대로 흘린다.
- 아래 「진행 중계」 절은 **형식**을 정하지 말투를 정하지 않는다. 제목 줄·표를 요구해도 말투는 이 절이 정한다.
- 질문은 `~나요?` · 청유는 `~ㅂ시다` · 줄표(`—`) 앞도 종결어미다(섞이면 반존대).
- ★ **호칭은 「사용자」다** (2026-08-26 사용자 판정 · 공용 「규칙/말투.md」 「호칭」 절이 정본).
  리포 문서·커밋이 3인칭으로 쓰던 말을 채팅에도 그대로 쓴다 — 층마다 다른 말을 쓰면 옮길 때 갈린다.
  쓰지 않는 말: 「형님」・「님」・이름. 근거는 격식은 괜찮지만 담백한 쪽을 선호한다는 것이고,
  이 한 줄이 말투 전반을 정한다 — **꾸미는 말·완충어·과한 공감을 덜어내는 쪽이 더 나은 글이다.**

## ★★★ 진행 중계를 쓰지 않는다 — 토큰 규율 (2026-08-12, 사용자 지적)

> 진행 과정을 중간중간 보여주는 것은 "일을 한다고 티내는 것"으로만 보이고, 토큰만 잡아먹는다는 지적(다음 달부터 낮은 요금제로 옮긴다).

- **도구 호출 사이에 해설을 넣지 않는다.** *"구조가 보입니다 —"*, *"자가 잡았습니다 —"*,
  *"세 결함 모두 해결됐습니다 —"* 같은 줄은 전부 군더더기다. **루프든 아니든 같다.**
- **말할 자리는 셋뿐이다:** ⑴ 사용자 판정이 필요할 때 ⑵ 방향을 바꿀 때(한 줄)
  ⑶ 최종 보고. 나머지는 **도구 호출과 커밋 메시지가 이미 말한다.**
- **★★★ 판정선은 「값어치」가 아니라 「채널」이다 (공용에서 받음 2026-08-15, 사용자 3회차 지적).**
  에이전트 내부의 연산·사고 수준일 뿐이지 사용자가 굳이 봐야 해서 나오는 말은 아니라는 지적.
  - **이 한 줄이 앞의 모든 조항을 대체한다.** *"이 말이 값어치가 있나"* 를 물으면 자는 언제나
    «있다» 고 답한다 — 자기가 방금 알아낸 것이니까. **물어야 할 것은 «이건 누구에게 하는
    말인가»** 다. 자기 다음 수를 정리하는 말이면 그건 **사고**이고, 사고에는 이미 제 채널이
    있다. 채팅은 **사용자가 읽어야 하는 것**만 들어가는 자리다.
  - **가르는 법:** 그 문장을 지웠을 때 **사용자가 할 일이 달라지나.** 안 달라지면 사고다.
    *"`container_settings` 가 훅 경로만 바꾼다"* 는 내 다음 수를 바꾸지 사용자의 수를 안 바꾼다 → 사고.
    *"9건 중 4건은 훅이 안 보여 줘서 추측할 뻔했다"* 는 사용자가 판정 범위를 다시 봐야 한다 → 채팅.
  - **★ 「…합니다 · …겠습니다 · …봅니다」로 끝나는 예고는 통째로 금지다.** 할 것이면 그냥
    하면 된다. 예고는 값어치를 잴 것도 없이 **전부 사고 쪽**이다.
  - **기본은 0회다.** 「필요하면 한다」로 두면 매 배치마다 하나씩 붙는다.
- **★★ 재는 자가 붙었다 — `tools/audit_session_cost.py` 의 「진행 중계」 (2026-08-15).**
  도구 호출이 딸린 응답에 들어간 말을 센다(도구 없는 응답의 말은 최종 보고라 안 센다).
  **문서로 두 번 고치고도 세 번째 지적이 왔으므로 규율 7 대로 기계로 옮겼다.**
  실측 첫 판(최근 3세션): **전공정리 82회·21,164자 · XSanity 112회·10,338자 · 공용 58회·9,114자.**
  ★ **목표치로 쓰지 말 것** — 판정이 필요한 자리(⑴)는 세어도 되고, 0 을 만들려다 물어야 할
  것을 안 물으면 더 나쁘다. **자기 과거와의 추세로만** 본다.
- **★★ 「대신 무엇을」이 없어서 이 조항이 안 지켜졌다 (2026-08-15, 같은 지적 2회차).**
  진행 상황을 계속 나열하기만 하면 "그래서 뭐 어쩌라는 건지" 알 수 없다는 지적 — 작업은 맡기고 보고는 결론만, 혹은 큰 단위로 짧게 받고 싶다는 취지였다.
  - **금지의 판정선은 「무엇을 했나」다.** *"확인합니다"* · *"고쳤습니다"* · *"찾았습니다"* ·
    *"이제 …합니다"* 는 전부 **행위 보고**이고, 그건 도구 호출 줄이 이미 보여 준다.
  - **★ 중간에 말해도 되는 것은 「무엇을 알게 됐나」뿐이다** — 사용자의 다음 판단을 바꾸는 것.
    **사실이 없으면 입을 다문다.**
  - **단위는 「도구 호출」이 아니라 「배치」다.** 5~10회 굴러 **덩어리 하나가 끝났을 때** 한두 줄.
    회차마다 한 줄씩 붙이면 그게 곧 중계다.
  - **끝나고 나서 과정을 되짚지 않는다.** *"세 번 틀렸고 이렇게 고쳤습니다"* 는 **커밋 메시지와
    공용 `변경일지.md` 의 몫**이다 — 채팅에 또 쓰면 같은 말을 두 번 사고 파는 것이다.
    채팅 최종 보고는 **결론 · 사용자가 판정할 것 · 안 한 것과 그 사유** 셋이면 끝난다.
- **★ 하네스가 내는 「편집됨 파일 N개 +x −y」 요약은 낭비가 아니다** (사용자 판정 — 이건 중간중간 실제 작업이 되고 있는지 확인하는 용도라 낭비가 아니라는 취지).
  내는 것**이고, 사용자가 진행을 확인하는 창이다. 줄일 것은 **내가 덧붙이는 해설**이지
  변경 사실 자체가 아니다 — 둘을 헷갈려 편집을 숨기거나 뭉뚱그리지 말 것.
- **★★ 하겠다고 말한 것을 조용히 안 하지 않는다** (2026-08-12, 사용자 지적 — 수정하겠다고 말해 놓고 실제 이력이 안 남았다는 지적). 감사 도구 개조를 *"다음 회차에 닫는다"* 고 적어 놓고, 비용이 커 보이자
  습관 규칙으로 **말없이 바꿔치웠다.** 판단을 바꾸는 것은 괜찮지만 **바꿨다고 말하지 않는 것**은
  안 된다 — 읽는 사람은 **안 한 것과 구별할 수 없다**(규칙 11 의 연장).
  → 예고한 것을 안 하기로 했으면 **그 자리에서 한 줄로 밝히고** 인박스에 옮긴다.
- **실측 낭비 목록(2026-08-12, 이 세션에서 센 것):**

  | 자리 | 회차당 | 처방 | 상태 |
  |---|---|---|---|
  | 빌드 출력(면제·사유 덤프) | ~2k | `build_site.py --quiet` | **넣음** |
  | 진행 중계 문장 | 300~600 | 위 규율 | **이 절** |
  | 재예약 프롬프트에 계획 전문 | ~800 | 계획은 인박스, 예약은 포인터만 | **넣음** |
  | 회차 보고 | ~500 | 보고는 루프 끝에 한 번 | **넣음** |
  | 감사 도구 전체 덤프 | 1~3k | `audit_figure_balance.py --fail-only`(참고 절 5개 생략) | **넣음** |
  | 커밋 메시지 장문 | 300~500 | 인박스에 있는 것은 **가리키고** 요지만 | **넣음(습관)** |
  | 넓은 `Grep` 컨텍스트(`.{100}…`) | 회당 수백 | 창을 좁히고 `-o` 로 | **넣음(습관)** |

## ★ 공통 시스템은 `Documents\naru` 에도 반영한다 (2026-08-12, 사용자 지시)

- 사용자가 시스템 관련 파일을 모아 둘 폴더(나루)를 만들고, 새 프로젝트를 시작할 때 여기서 참조·재사용하라고 지시했다(공용 시스템을 수정하면 이 폴더도 함께 갱신).

- **자리:** `~/Documents/naru/` — `규칙/`·`훅/`·`도구/`·`기록/` + `README.md`.
  ★ **2026-08-15 에 `Claude` → `naru` 로 이름이 바뀌었다.** 경로를 코드에 적을 때는
  `shared_sync_check.SHARED` 를 임포트한다 — 그것이 **둘 다 보고 환경변수를 존중하는 유일한 자리**다.
  무엇을 왜 넣었는지는 그 README 가 정본이다.
- **★ 부르는 말은 「나루」다 (2026-08-15).** 프로젝트 사이 보고에서 이 폴더를 가리킬 때
  «나루한테 보냈다»·«나루에서 받았다»로 쓴다. 미러는 파일을 나르지 **부르는 말은 안 나르므로**
  이 줄이 각 리포에 손으로 들어간다(공용 `변경일지.md` 「각 프로젝트가 할 일 ⑵」). 이름의 근거는
  그 README 머리에 있다 — `Claude` 는 도구·모델·회사를 함께 가리켜 흐려지고, `나루` 는 전 리포
  grep 0건이라 검색으로 정확히 걸린다.
- **★ 트리거는 기계다 — 손으로 복사하지 않는다 (2026-08-14 개조).** `sync_common.py` 가
  main 반영을 끝낸 자리에서 **공용 폴더까지 스스로 민다**(`mirror_shared`). `commit.py` 가
  `sync_common` 을 부르므로, **커밋 하나로 리포 → main → 공용**이 한 동작이다.
  - **무엇을 옮길지는 `docs/shared-mirror.txt` 대장이 정한다.** 우리 「공통」은 공용의 기준보다
    넓어서 통째로 밀면 그 폴더가 이 프로젝트의 사본이 된다 — 공용의 기준은
    *«프로젝트 이름·도메인을 몰라도 성립하는가»* 이고 그 폴더 `변경일지.md` 가 정본이다.
  - **공용 쪽이 우리 모르게 바뀐 파일은 덮지 않고 신고한다** — 남의 수정을 소리 없이 지우지
    않기 위해서다. 받아올지는 사람이 판정하고, 알림은 `shared_sync_check` 훅이 띄운다.
  - **기계가 못 하는 것은 `변경일지.md` 의 «왜 바꿨나» 한 줄**이다. 그건 계속 사람이 쓴다 —
    이 도구가 보증하는 것은 «복사했는가» 하나뿐이다(그 이상을 적으면 과잉 주장이다).
  - 잠금 `test_checks.py::test_shared_folder_mirror_is_machine_driven`.
- **레포 밖 쓰기 예외다.** AGENTS 규칙 9 는 쓰기를 리포와 스크래치패드로 묶어 두는데,
  이 경로는 **사용자가 그 용도로 만들어 명시적으로 지시한 자리**라 그 예외다. 다른 경로로
  넓히지 않는다.
- **과목·교재에 딸린 것은 넣지 않는다** — 갈라져야 하는 것은 각 프로젝트가 갖는다.

## Claude 전용 메모

- 사용자 취향·진행 상태는 Claude 자동 메모리에 있다. **AGENTS.md 내용을 메모리에 복제하지 말 것** —
  역할이 다르다(AGENTS = 리포 규칙, 메모리 = 사용자 취향·진행 상태).
- 배치 실행·승인 절약 규율은 **AGENTS.md `## 실행 규율`** 에 있다.
  지적 모아 한 번에 / 감사 통합 / 승인 횟수 사전 보고 / 정확 명령 그대로 실행 /
  **파일 편집도 한 번에 몰아서** / **토큰 걱정으로 잘게 끊지 말 것** — 여기 중복하지 않는다.
- 감사는 `tools/audit_content.py`(읽기 전용), 워크오더 수용은 `tools/verify_workorder.py`,
  세션 닫기 전 `tools/close_report.py`.
- (보관) Codex 설정·복붙 문서는 `docs/codex-setup.md`·`docs/handoff-prompts.md`, 분업·핸드오프 원문은
  `docs/archive/codex-workflow.archived.md` — 재분업할 때만 본다.

# Compact instructions

> 공식 기능이다(`code.claude.com/docs/en/costs`) — 자동·수동 압축이 **무엇을 남길지** 읽는 자리.
> 계획·판정을 **파일**(인박스·워크오더)에 두는 것이 정본이고(AGENTS 2026-08-13 «채팅에만 두면
> 진다»), 이 절은 그 파일을 **가리키는 실**이 압축에서 끊기지 않게 하는 보조다.

압축할 때 아래를 반드시 남긴다 —

- 지금 열려 있는 **워크오더·인박스 파일 경로**와 **몇 번 항목까지 닫았는지**
- **변경점 기준선**에 관한 것 — `reviewHold` 선언 여부, 아직 안 민 챕터, 「커밋 → 빌드 → 민다」 중 어디인지
- 이번 배치에서 **고쳤다고 보고한 것**과 **아직 안 고친 것**의 구분(규칙 11)
- 사용자가 내린 **판정**과 그 말 그대로의 인용 — 판정은 요약하면 근거가 사라진다
