# CLAUDE.md — 전공정리프로젝트 에이전트 지침

> **이 저장소를 방금 받았다면(툴킷 첫 세션).** 이 파일은 참고 자료가 아니라 **이 폴더에서 일하는 규칙**이다 —
> 그대로 채택한다. `data/` 에 과목이 없으면 `START.md` 순서대로 사용자와 첫 과목을 세운다.
> 아래 본문은 원작자 실사용본이라 21과목·배포·「공용 폴더」 이야기가 섞여 있다 — 없는 것은 건너뛴다.

- **Claude Code의 상세 규칙 정본은 이 파일이다.** Codex/Astra는 루트 `AGENTS.md`의 짧은 어댑터를 함께 따른다 —
  공통 불변조건은 여기에, Codex 작업 시작·재개 규율은 어댑터와 `docs/작업-재개-기록.md`에 둔다.
- 이 문서 = **작업 중 항상 유효한 불변조건.** 유효한 것만 둔다. 분리 문서는 **링크만** 한다(`@import` 하지 않는다).
- **전 프로젝트 공통 판정선은 사용자 층 `~/.claude/rules/층0-판정선.md` 가 매 세션 싣는다** — 말투 · 진행 중계 ·
  물을 때·끝낼 때 · 증거 · 커밋·푸시 · 요청한 만큼만 · 고치기 · 산출물 · 자격증명. 여기엔 **이 리포 고유의
  판정선과 도구 이름만** 둔다(공용 폴더 변경일지 2026-09-11 「이식 절은 지워도 된다」).
- **분업·전체 검사·새 범위 전에는 [배치 작업](docs/배치-작업.md)을 따른다.** 사용자 지정 범위와 현재 ready 후보를 먼저 확인한다.

**★ 문서 배치 원칙 — 「올릴 자격」** (신설 2026-08-14 · 소급 적용 2026-08-16)

- **파일을 열기 *전에* 알아야 하는 것만 이 문서에 둔다.** 열고 나서 필요한 것은 `docs/**` 나 검사
  독스트링에 둔다 — 거기 있다고 잊히지 않는다(검사는 돌고, 문서는 필요할 때 읽힌다).
- **근거는 토큰이 아니라 주의력이다.** 이 문서는 매 세션 통째로 실리고, 너무 길면 절반이 무시된다.
  실측: 2026-08-14 하루에 닫힌 결정을 두 번 다시 열었다 — 여기에도 공용 문서에도 적혀 있었는데도.
- 조항을 추가할 때 자문한다: *[발화 생략]* — 몰라도 되면 안 넣는다.
- 표는 `docs/context-budget-plan.md` 의 「내려도 되는 것 / 반드시 남길 것」.

**★★ 크기 — 재는 자가 정본이고, 「삭감」과 「이관」 둘 다 열려 있다**

- **크기를 여기 손으로 적지 않는다** — `python tools/audit_guide_size.py .` 가 정본이다(래칫).
  문서 안에 박은 수는 반드시 낡는다(*[발화 생략]* 라고 적힌 채 214KB 가 되도록 아무도 안 쟀다).
- **삭감**(2026-08-16 사용자 판정 *[발화 생략]*): 단위는 [발화 생략]가 아니라
  **«옮기고 실을 남기기»** 다 — **포인터(원장 날짜 · 도구 이름) 없이 지우지 않는다.** 남기는 것은 셋:
  **규칙 · 판정선 · 전제.** 가는 곳도 셋: 사용자 지적 → 공용 폴더 `기록/feedback-ledger.md`
  (조회 `tools/feedback_lookup.py`) · 실측 → 그 도구의 독스트링 · 공용 규칙에 정본이 있는 것 → 포인터 한 줄.
- **이관(`docs/` 로 내리기)은 2026-08-16 에 열렸다** — 공용 순서 ⑵(공용 폴더 `변경일지.md`).
  **판정선: 「기계가 이미 막는 절은 안 실어도 안 뚫린다」**, 그때 「막는 자」는 **훅 deny 또는
  exit≠0** 이다(세기만 하는 자는 아니다 — 실행 규율 17). 내린 절에는 **무엇을 기계가 안 막는지**와
  **읽는 법(Grep)** 을 남긴다. 첫 이관: 삽화 규격 · 콘텐츠 표기 규약.
- 옛 판정(2026-08-12 *[발화 생략]*)은 *«쪼개도 세션 시작에 전부 로드되니 토큰을 안 아낀다»* 를 말한
  것이라 **`@import` 분할**에만 걸린다. `docs/` 이관은 실제로 안 실리므로 그 판정의 대상이 아니다.
- 설계 정본 공용 폴더 `기록/AGENTS-삭감-목록.md` · 실행 측 `docs/AGENTS-삭감.workorder.md` ·
  간결체 규약 공용 「규칙/실리는-글은-짧게.md」(실행 규율 20).


## 프로젝트 개요

한 대학 재학생의 전공 과목 학습 자료를 데이터(JSON) + 단일 뷰어 템플릿으로 만드는 프로젝트.
**어떤 과목·교재·경로인지 같은 과목별 세부는 `data/<과목>/SUBJECT.md` 에 있다** —
여기(공통)에는 과목 무관한 것만 둔다.

```
data/<과목>/chNN.json              ← 챕터 콘텐츠 (과목별)
data/<과목>/*.workorder.md         ← 작업지시서 + 진행 기록 (과목별)
site/template/viewer.template.html ← 뷰어 원본 (공유, 유일한 수정 지점)
site/_assets/viewer.<해시>.css|js  ← 빌드가 뽑아낸 공유 뷰어 (산출물, 커밋 안 함)
site/fonts/*.woff2                 ← 전 과목 공용 웹폰트 (공통, main이 정본 · 커밋한다)
site/<과목>/chNN.html              ← 빌드 산출물 (과목별)
tools/build_site.py                ← 빌드 + 내장 검사 (공유)
tools/build_review.py              ← 변경점 하이라이트 리뷰 빌드 (공유)
tools/audit_content.py             ← 읽기 전용 콘텐츠 감사 (공유)
```

**경로 규칙 넷** — 그 파일을 `Read` 할 때만 실린다: `content.md`(장 JSON) · `figures.md`(장 JSON·삽화 생성기) ·
`tools.md`(`tools/**`·훅 — 공통 도구에 과목 사실 금지 · 인코딩 함정) · `viewer.md`(템플릿 — 자리표는 첫 `<script>` 에만).
안 실린 세션에서 그 경로에 새로 쓰면 `guard_write` 가 막는다 — 같은 종류 파일 하나를 먼저 `Read` 한다.

## ★ 공통 vs 과목별 — 무엇을 함께 봐야 하나 (2026-09-06 개정)

브랜치·워크트리 분리가 없어져 **「사는 곳」이 아니라 「고칠 때 무엇이 함께 걸리나」**의 구분이다.

| 구분 | 파일 | 고칠 때 |
|---|---|---|
| **공통 = "어떻게 일하나"** | `CLAUDE.md`·`.claude/**`·`tools/**`·`site/template/**`·`docs/**` | **전 과목에 즉시 적용된다** — 저장하는 순간 21과목이 그 규칙 아래 놓인다 |
| **과목별 = "무엇을 다루나"** | `data/<과목>/**`(`SUBJECT.md` 포함)·`site/<과목>/**` | 그 과목만 바뀐다 |

- **공통을 고치면 그 자리에서 전 과목이 받는다.** 예전처럼 21번 `merge` 할 것이 없다 —
  그 수동 전파가 안 돼서 "다른 과목은 고쳐졌는데 여긴 안 됐다"가 반복됐고, 그게 합친 이유다.
- **공통 규칙을 새로 만들면 그 자리에서 소급도 본다**(규칙 7 ⑵⑶) —
  `python tools/audit_convention_drift.py` 로 **이미 쓴 콘텐츠**에 후보가 남았는지 훑는다.
  새 규칙은 앞으로만 지키고 과거는 방치되는 것이 이 리포의 반복 결함이다.
- **세션이 열릴 때 「선 자리」를 훅이 찍는다** — `.claude/hooks/session_brief.py`.
  사실만 낸다(판정하지 않는다). 잠금 `test_session_brief_states_facts_not_guesses`.
- **공용 폴더(공용 시스템)와의 동기화는 여전히 사람 판정이다** — 신고가 뜨면 그 자리에서 판정해
  가져오고 사후보고 한 줄(아래 「공통 시스템은 공용 폴더에도」 절).
## 절대 규칙 (위반 시 파이프라인/원칙 붕괴)

1. **빌드 산출물 손편집 금지.** `site/*/**.html` 은 빌드가 덮어쓴다. 뷰어 수정 =
   `site/template/viewer.template.html`, 콘텐츠 수정 = `data/**/*.json`, 그 뒤 `python tools/build_site.py`.
   검사 실패는 데이터·템플릿을 고쳐 통과시킨다(검사 완화 금지).

2. **콘텐츠 무단 창작 금지(no-fabrication).** 이론·공식·문제·pitfall 은 교재 대조 또는 오답로그·사용자
   제보가 근거여야 한다. `pitfalls[].source` 의 접두사는 **`textutil.PITFALL_BOOK_BY_SUBJECT` 가
   정본**이다 — 과목마다 등록된 교재(`"<저자> 본문 경고 (...)"`) 또는 `"오답로그 e0NN"` ·
   `"사용자 제보 (날짜)[발화 생략]… 경고 (해당 없음, …)"`)는 빌드가 막는다(`pitfall_source_issue`).
   근거를 확인할 수 없으면 만들지 말고 먼저 묻는다.

3. **교재 원문 저장 금지(저작권).** `error_log.json` 에는 문제 번호 참조와 진단만.
   교재 문장을 데이터에 복사하지 않는다(자작 요약·변형만).

4. **수치는 스크립트로 독립 검산.** 답·중간값을 눈으로 계산해 적지 않는다.

5. **홈의 Claude 메모리(`C:\Users\<사용자>\.claude\**`)는 건드리지 않는다.** 프로젝트 안의 `.claude/` 는
   대상이 아니다.

6. **`git push` 는 배포가 아니다.** 배포는 `tools/deploy_all.py` 가 찍는 `vercel deploy` 수동 실행뿐이고,
   프로덕션은 `--prod` 가 있어야 돌며 **사용자가 명시적으로 요청할 때만** 쓴다. 로컬 커밋·push 는
   자유롭게 해도 된다. 언제 미느냐의 판정은 실행 규율 14 가 정본이다.
   > 전제: Vercel 프로젝트를 GitHub 연동 없이 만들어 **자동 배포가 없다**는 사실에 걸린 조항이다
   > (`vercel git disconnect` 로 확인됨). 옮길 때는 조항이 아니라 이 전제를 먼저 다시 잰다.
   > ★ **`vercel deploy` 실행 자체는 매번 실시간 승인이 필요하다**(2026-09-13 실사고 — 사용자가
   > 잠든 사이 실행이 permission-denied 로 막혔는데 완료를 기다리며 대기해 루프가 안 끝났다).
   > **"배포 준비하고 종료해라"는 지시는 `deploy_all.py` 로 번들·명령까지만 만들어 두고 그 자리에서
   > 끝내라는 뜻이지, 실제 `vercel deploy` 승인·완료를 기다리라는 뜻이 아니다** — 그 승인은 에이전트가
   > 못 하므로 기다려도 끝나지 않는다. 종료가 예약된 상태에서 사용자가 [발화 생략]류 말을 하면
   > 그 자리에서 카운트다운부터 멈추고 확인한다.

7. **지적은 「부류」로 고친다 — 인스턴스만 고치는 것은 위반이다.**
   ⑴ 왜 그렇게 지시했는지 원칙을 먼저 뽑고 ⑵ 그 원칙에 걸리는 같은 부류 전체를 전 챕터에서 감사해
   ⑶ 한꺼번에 고친다. 똑같은 상황이 아니라 비슷한 케이스도 같은 논리로 적용한다.
   - **기본값은 부류다 — 인스턴스는 증명해야 하는 예외다.** 순서: ⓐ "이건 무슨 부류인가"를 말로 적는다
     ⓑ 그 부류를 전 챕터에서 센다 ⓒ 한 건뿐이고 구조상 더 나올 수 없을 때만 인스턴스로 처리한다.
   - 인스턴스로 처리했으면 그 판단을 밝힌다 — "부류로 봤으나 N건 중 1건이고 재발 경로가 없다"(규칙 11).
   - **재발이면 원인과 방지장치까지 답한다.** 원인은 "빠뜨렸다"가 아니라 **빠뜨려도 통과되는 구조**를
     지목해야 하고, 조치는 기계여야 한다(빌드 lint 승격 · 기본값 뒤집기 · 레지스트리화).
     "다음엔 주의"는 조치가 아니다.
   - **구현 전 체크포인트** — 워크오더나 첫 진행 보고에 한 줄씩:
     `사용자가 든 사례` → `실제 원하는 결과` → `동일 부류 감사 범위` → `재발 여부·구조적 원인` →
     `제안안 평가·더 나은 대안` → `이번에 추가할 기계적 방지장치`. 해당 없음도 이유를 쓴다.
   - 새 챕터·검수 때는 `data/*/chapter-quality-contract.md` 를 함께 읽고 **모든 챕터**에 같은 기준이
     계승됐는지 **파일을 직접 봐서** 확인한다(워크오더의 "완료" 문구로 판정하지 않는다 — 미반영 2회).
   - **부류 판정은 과목 경계를 넘는다.** 처방이 삽화·표기·폰트·규칙 중 **어느 소재든, 뷰어·빌드처럼
     전 과목이 공유하는 「만드는 방식」의 상수·규칙**이면 "메인에 올릴 만큼 크냐"를 재지 않고 그 자리에서
     공통 도구(`tools/fix_*.py`·`buildlib/checks_*.py`)나 공통 문서(`docs/*-규격.md`)로 올린다.
     "이 과목만의 사정"은 처방이 **과목의 사실**(교재 내용·범위)을 다룰 때만 유효하다 —
     크기가 아니라 종류로 가른다.
     - **⑵(공통 도구·문서 신설)를 만드는 동시에 ⑶(`tools/audit_convention_drift.py` 에 그 규칙을 재는
       체크 함수 등록)도 만든다.** 새 도구는 앞으로 쓸 때만 막고 **이미 있는 다른 과목의 낡은 콘텐츠는
       안 본다** — 등록을 빠뜨리면 새 규칙은 미래만 지키고 과거는 방치된다.
     - 소급이 필요한 것은 **⑵는 있는데 ⑶이 없는 경우**뿐이고, 그 목록은 `docs/도구-등록부.md` 에 이미
       유한하게 나열돼 있다(도구화를 안 한 1회성은 위 "기본값은 부류다" 절차가 이미 처리한다).
     - **⑷ CLAUDE.md 의 트리거 줄도 같은 자리에서 넣는다 — ⑵·⑶은 「이미 쓴 것」만 본다,
       「지금 쓰는 중」은 못 본다.** ⑶은 사후 감사라 **새로 쓰는 중인 세션은 못 잡는다** — 쓰는 순간에
       알아야 막힌다. 통째로 싣지 말고 필요한 절만 가리킨다(「올릴 자격」).
   → 원장 2026-08-07 · 2026-08-29.

8. **지적을 무작정 수용하지 않는다 — 분류하고 입장을 밝힌다.** 사용자 지시는 대개 *기존에 사용자 요구로
   만든 것*을 고치라는 것이라, 받아적기 전에 검토한다.
   - 분류: ⑴ **결함**(깨짐·오류·물리적으로 틀림) → 즉시 수정 ⑵ **설계 판단**(무엇을 보여줄지·배치·기능)
     → 입장 표명 의무 ⑶ **취향** → 대안 제시 후 사용자 선택.
   - 배치 끝에 카운트 보고: "N건 중 결함 X · 설계판단 Y · 반박 Z". **설계 판단이 있는데 반박 0이면
     경고 신호**다 — 왜 전부 타당했는지 해명한다.
   > 전제: 큰 배치가 오면 속기 모드에 빠져 판단이 밀려난다. 목적은 완결성이 아니라 검증이다.

9. **쓰기는 허용된 두 곳에만**(권한 규칙이 못 막는 마지막 층).
   - 쓰기 = ⑴ 프로젝트 루트 안 ⑵ 에이전트 전용 세션 스크래치패드. 그 외에는 어디에도 쓰지 않는다 —
     바탕화면·내 문서·홈·교재 폴더 전부.
   - 읽기 = 프로젝트 루트 + 교재 폴더(`additionalDirectories`) + 스크래치패드.
   - 경로가 어떻게 생겼든 적용된다: 리디렉션 · 출력 경로 인자(`pdftotext … 경로`, `sort -o 경로`) ·
     파이썬 `open(경로,"w")`.
   - 두 곳 밖에 써야 하면 하지 말고 먼저 묻는다.
   > 전제: 리디렉션은 명령이 아니라 셸의 성질이라 권한 규칙은 `>` 대상이나 `open()` 을 못 본다.
   > **그래서 이 규칙이 실질 경계다 — 규칙이 통과시켰다는 게 해도 된다는 뜻이 아니다.**

10. **할 수 있는 것/없는 것은 에이전트가 먼저 선언한다.** 작업을 받으면 착수 전 첫 응답에서 3구역으로
    분류해 선언한다. 형식: `구역: 초록 N건 / 노랑 N건 / 빨강 N건 — <빨강 항목과 이유>`.

    | 구역 | 범위 | 행동 |
    |---|---|---|
    | **초록** | SVG 좌표·CSS·리팩터·빌드 스크립트 확장·감사 실행·빌드가 이미 지적한 것 수정 | 그냥 한다 |
    | **노랑** | 부류 범위 판단, 결함/설계 분류, 배치·레이아웃 선택 | 한다. 단 `## 자율 판단 기록`에 남긴다 |
    | **빨강** | 출처 없는 콘텐츠 창작, 콘텐츠 삭제, 배포·공개 설정 변경, `error_log.json` 수정, 검사 완화·우회 | 하지 않는다. 못 하는 이유와 필요한 것을 적는다 |

    - 콘텐츠 빨강의 기준은 「출처가 있는가」다: 출처 없는 창작은 빨강, **교재 대조를 마친 작성은 빨강이
      아니다**(이론·유도·연습·삽화 전부 내 몫).
    - 노랑의 승인자는 사용자다. 그 자리에서 받으면 `(사용자 승인: 날짜/출처)` 로 기록하고 진행한다.
      사용자 부재가 작업을 멈추는 이유가 되어선 안 된다.
    - 자기 구역 밖 작업을 조용히 진행하지 않는다.

11. **판정은 명령으로 뒷받침한다 — 미검증 주장을 검증된 것처럼 쓰지 않는다.** "통과"·"N건 전부 확인"·
    "이상 없음" 에는 그것을 산출한 명령을 함께 적는다.
    - 명령을 댈 수 없으면 `육안 확인(미검증)` 이라고 표시한다. 미검증과 검증이 겉모습이 같으면 읽는
      사람은 구별할 방법이 없다.
    - 수량 주장은 스크립트 출력에서 온 것이어야 한다. 세지 않고 "대부분"·"1위"·"압도적"을 쓰지 않는다.
    - 감사를 근거로 쓸 때는 **그 스크립트의 순회 범위부터 확인한다.** 범위를 모르는 "0건"은 "없다"가
      아니라 "거기까지는 없다"이다.
    - **명령을 붙여도 틀리는 자리 — 「자」부터 의심한다:**
      - 새 자의 첫 실행은 「재는 것」이 아니라 「자를 재는 것」이다. 첫 출력을 결론으로 쓰지 말고
        *이 자가 무엇을 못 보나* 를 먼저 적는다.
      - 순회가 잘렸는지 먼저 본다. 상한에 걸린 「0건」은 「거기까지는 없다」이다.
      - 같은 값을 두 가지 방법으로 재 본다. 한 자만 쓰면 틀렸을 때 알 방법이 없다.
      - 명령으로 안 닫히는 넷은 사람이 본다: 뷰어 JS·CSS 동작 · 삽화 렌더 · 리포 밖 파일 비교 ·
        「결과가 그럴듯해서 그냥 넘어가고 있지 않은가」. 자리를 비우고 목표만 남기는 운용(`/loop`)일수록
        마지막 항목이 커진다.
    - 기계로 검사할 수 있는 것은 검사로 만든다(빌드 검사·`tools/audit_content.py`). 없는 것은 미검증이라고
      밝힌다. 둘 중 하나여야 하고 조용한 단정은 없다.
    → 원장 2026-08-14.

12. **독자성 — 이 자료는 번역본도 요약본도 아니다.** 절 순서 · 예시 소재 · 삽화 · 연습문제 · Moran 사용의
    판정선은 경로 규칙 `.claude/rules/content.md`(장 JSON 을 `Read` 할 때 실린다). 기계 `audit_content.py`.

## 콘텐츠를 쓸 때 — 경로 규칙 `.claude/rules/content.md`

- 장 JSON 을 열면 실린다: 규칙 12 독자성 · 내부 링크 · 표기 규약 포인터 · 목표 독자 · 문제 설계 ·
  과목 개요·도입부 · 데이터 함정(LaTeX·JSON·위첨자·아래첨자).
- 그 규칙이 안 실린 세션에서 장 JSON 을 쓰면 `guard_write` 가 막는다 — 먼저 그 장 파일을 `Read` 한다.
- 링크 문법 `[[chNN:앵커id|문구]]` · 과목 간 `[[과목폴더@chNN:앵커id|문구]]` — 좁은 앵커로 착지시킨다.

## 실행 규율 — 승인 피로와 토큰 낭비 막기

> **규칙·판정선·전제만** 둔다. 경위·실측은 원장(공용 폴더 `기록/feedback-ledger.md`,
> 조회 `python tools/feedback_lookup.py "<요지>"`)과 각 도구 독스트링이 정본이다.
> 번호는 코드·문서가 인용하므로 **바꾸지 않는다**(1·3·6·8 은 사용자 층과 겹쳐 줄였다 — 2026-09-11 공용 폴더 제안).

1. **지적은 모았다가 한 번에 처리한다** — 수집 목록에 쌓고, 사용자가 [발화 생략]할 때 감사 → 수정 → 빌드를 한 번에.
   > 전제: 지적 1건마다 같은 부류 전수 감사가 붙는다(규칙 7) — 10건을 따로 주면 10번 돈다. → 원장 2026-07-27.

2. **감사는 하나로 합쳐 한 번만 돌린다.** 필요한 감사를 전부 설계한 뒤 한 스크립트로 실행한다.
   일회성 `python - <<EOF` 를 쪼개 반복하지 않는다. 또 쓸 것 같으면 `tools/` 도구로 승격한다.

3. **착수 전 예상 승인 항목과 횟수를 보고한다** — 예상 밖 임의 스크립트는 실행 전에 승인받는다.

4. **정확 허용된 명령에 리디렉션·후처리를 붙이지 않는다.** 승인창이 떴으면 실제로 걸린 하위 명령을
   확인하고 추측으로 단정하지 않는다(규칙 11).
   - **파일 편집도 배치로.** 한 파일을 `Edit` 로 나눠 부르지 말고 최소 횟수로 몰고 한 번만 보고한다.
     → 원장 2026-07-27.
   - `Write` 가 한 턴 출력에 안 들어갈 문서는 `python tools/replace_doc_section.py` 로 절 단위 교체
     (잘린 `Write` 는 파일을 깨뜨린다).

5. **위 1~4는 `.claude/hooks/guard_bash.py` 가 막는다.** 차단 목록과 경위는 그 파일 `deny_reason()` 이
   정본이다 — 여기 복사하지 않는다.
   - 새 스크립트를 짜기 전에 `tools/` 의 기존 도구가 그 검사를 하는지 확인한다.
   - 훅이 거부하면 형태를 바꿔 다시 짠다. 끄거나 우회하는 것은 빨강(규칙 10).
   - 잠금은 권한이 아니라 테스트다 — `test_checks.py::test_guard_rules`. 판정은 전부 `deny_reason()`
     안에 둔다(`main()` 에 쓰면 테스트가 못 본다).

6. **토큰 걱정으로 작업을 잘게 끊지 않는다** — 판정선은 사용자 층 [발화 생략]. `/loop` 회차 규칙은 스킬 `loop-round`.
   > 전제: 현재 max 플랜이라는 **사실**에 걸린 조항이다. 한도가 좁은 환경으로 옮기면 이 전제를 먼저 다시 잰다
   > (사용자는 다음 달 낮은 요금제로 옮길 예정). → 공용 폴더 `규칙/끊지-말고-끝내고-보고한다.md`.

7. **반복되는 승인 프롬프트는 기계로 고친다.** 같은 명령이 새 세션마다 뜨면 기억이 아니라 설정·가드의
   문제다. 원인을 실측해 규칙을 바꾼다.
   - 읽기 전용 git → `guard_bash.SAFE_GIT`. `checkout·restore·reset·rebase·push` 는 게이트 유지 —
     단 `push` 의 근거는 「게시라 못 되돌린다」 하나라 **비공개 원격에서는 성립하지 않는다**(규율 14).
   - 미커밋 변경 되돌리기는 `python tools/revert_files.py <경로…>`. 브랜치 전환·리셋·강제·`.`·`-A` 는
     거부한다 — 게이트의 근거 자체를 없앤 것이라 완화가 아니다.
   - 커밋은 `python tools/commit.py "<메시지>" <경로…>`, 공통 반영은 `python tools/sync_common.py` —
     둘 다 git 을 subprocess 로 돌려 승인을 안 탄다. 손으로 `git checkout … 파일나열` 하지 않는다.
   - 무엇이 공통인지는 `sync_common.common_paths_in()` 하나가 판정하고 끄는 깃발을 두지 않는다 —
     싫으면 커밋을 쪼갠다. 대장은 `docs/shared-mirror.txt`, 공용 쪽이 우리 모르게 바뀐 파일은 덮지 않고
     신고한다.
   - 프롬프트가 뜬 명령은 고치기 **전에** `docs/permission-prompt-ledger.md` 에 한 줄(없으면 재발인지
     첫 발생인지 구별할 수 없다). 「검증」엔 다시 실행해 본 결과만 적는다. 새 세션 시작 직후 그 원장 맨
     아래 '반드시 할 확인'을 실행한다.
   - blocking `PreToolUse` 훅은 permission `allow` 보다 먼저 평가되고 훅 deny 가 우선한다. 리포 도구는
     경로만 보지 않고 **HEAD 추적·blob 일치**까지 확인한 뒤에만 자동 허용한다.

8. **기록은 보고가 아니다 — 사용자에게 직접 보여야 끝난다**(보고 형식·[발화 생략]의 증거는 사용자 층 [발화 생략]·「증거」).
   이 리포 고유의 셋:
   - 「확인하실 자리」에는 **지금 검수하는 범위 안의 것만** 올린다(뷰어가 하이라이트를 안 보여 주는 컬렉션은
     대상이 아니다). 범위 밖 수정은 「고쳤음」 쪽에만. → 원장 2026-08-07.
   - 「무엇을 바꿨나」에 `부류 → 사용자 지적 → 무엇을 바꿨나 → 어디` 표를 붙인다. [발화 생략]은 보고에
     넣지 않는다(`## 자율 판단 기록` 파일의 것은 둔다 — 원장 2026-08-05).
   - 기계가 「닫았다」를 보는 자리는 `verify_workorder` 의 `## 완료 기준`뿐이다.

9. **루프 재예약은 훅이 감시한다 — 손으로 기억하지 않는다.** 켜기·끄기만 사람이 한다
   (`python tools/wakeup_guard.py on` · `off --reason user|blocked|done` · `status` — 문맥 크기는 멈춤 사유가 아니다). 루프 모드인데 그 턴에 `ScheduleWakeup` 이
   없거나 회차에 커밋이 0이면 Stop 훅이 멈춤을 막는다. 회차 규칙은 스킬 `loop-round`.

10. **의존이 없는 도구 호출은 한 응답에 묶어 부른다**(왕복마다 그때까지의 문맥이 통째로 다시 들어간다).
    - 묶는다: 서로 결과를 안 보는 `Grep`·`Read`·`Glob`, 다른 파일의 편집, 독립된 검사 실행.
    - 못 묶는다: 앞 결과가 다음 인자를 정하는 것 — 거기까지 묶으면 틀린 일을 빠르게 하는 것이다.
    - 묶음률을 목표 숫자로 삼지 않는다. 자는 `tools/audit_tool_parallelism.py`.

11. **게이트는 배치 끝에 한 번 돌린다 — 편집마다 돌리지 않는다.** 검사를 줄이라는 것이 아니라 시점을
    묶으라는 것이다(완화·우회는 빨강, 한 번도 안 돌리는 것은 미완).
    - 자리: `build_site.py` 는 데이터·템플릿을 고친 배치 끝, `test_checks.py` 는 검사·훅·도구를
      건드렸을 때, `close_report.py` 는 세션을 닫을 때 한 번.
    - 예외는 스스로 증명한다(깨지기 쉽거나 여러 파일에 걸친 수정). 금지가 아니라 기본값이다.
    - 기계: `.claude/hooks/gate_rerun_guard.py`(배선 확인 `--check-wiring .`).

12. **비싼 것은 왕복 수가 아니라 「도구 출력의 크기」다** — 한 번 들어온 큰 출력은 그 뒤 모든 왕복에서
    다시 읽힌다. 규율 10 보다 이쪽이 크다.
    - 처방 셋: ⑴ 파일을 통째로 읽지 않는다(`Grep` 으로 필요한 줄만, 창은 `-o` 로 좁게)
      ⑵ 감사·빌드는 `--fail-only`·`--quiet` ⑶ 바깥 문서는 필요한 값만 뽑아 온다.
    - **좁은 창은 「읽었다」가 아니라 「본 만큼」이다** — 표 한 행의 끝 칸에 지시가 오는 형식에서 앞 칸만
      보고 「확인 완료」로 표시하면 지시가 통째로 사라진다. → 원장 2026-08-16.
    - 스크린샷은 마지막 수단이다(1장 ≈ bash 354회).
    → 원장 2026-08-12.

13. **서브에이전트는 공짜 병렬이 아니다**(실측 2.6~5.9배). 이 리포는 사용자가 요청할 때만 쓰고, 값을
    하는 것은 범위가 크고 서로 독립인 하위 작업뿐이다. correctness-first, not speed-first —
    안 돌린 게이트는 낭비가 아니라 미완이고, 규율 11 로 게이트를 미루지 않는다.
    > 전제: 토큰 2.6~7배는 구조에서 오므로 어디서나 참이지만, 「그래서 안 쓴다」는 판정은 **병목이
    > 검수자 한 명의 시간**이라는 사실에 걸려 있다 — 리뷰어가 여럿인 곳에서는 깨진다. 병목을 먼저 잰다.
    - 하위 세션 배정 전 `get_usage` 70·85 % 문턱은 **해제됐다**(사용자 2026-09-17 *[발화 생략]*).
    - **메인 기본 Opus 중간, 근거 대조 판정·설계도만 Fable** — 전환은 사용자만 한다. 사용자 부재 중 모델 전환이
      필요한 판정은 급하지 않으면 인박스 ☐ 로 미룬다(사용자 2026-09-17 *[발화 생략]*).

14. **커밋·푸시는 묻지 말고 하고, 한 줄로 사후보고한다** — 묻는 자리 넷·보고 형식은 사용자 층 [발화 생략] 절.
    이 리포의 사실: 원격 공개 여부는 `gh repo view --json isPrivate` 로 잰다 · push 는 배포가 아니다(절대 규칙 6) ·
    `commit.py` 자신을 고친 커밋은 `commit.py` 로 못 올린다(HEAD 대조 가드) — 그때만 `git add`·`git commit` 을 손으로.
    → 정본 공용 「규칙/커밋-푸시-규약.md」.

15. **다른 세션에 메시지를 보내는 선 — 기본은 파일이다.** 파일 한 줄은 필요할 때만 읽히지만 메시지 한
    통은 그쪽 세션이 온전한 한 턴을 돌게 만든다.
    - 정당한 자리는 셋뿐이다: ⑴ 내가 그쪽 트리를 건드렸다 ⑵ 지금 깨진 것 위에서 돌고 있다
      ⑶ 그쪽 판정이 있어야 내가 못 나간다.
    - 보내지 않는다: 개선·수정 알림(→ 커밋 메시지·공용 `변경일지.md`) · 관측과 수치
      (→ `기록/세션비용.csv` 와 `cost_brief` 훅) · 「받았다·반영했다·고맙다」 회신.
    - 한 사안에 왕복 1회. 세 번째 왕복이 필요해 보이면 한쪽이 판정을 안 한 것이다.

16. **수치에는 근거를 붙인다 — 숫자는 근거가 없어도 「결정」처럼 생긴다.** 판정선은 둘 중 하나다:
    ⑴ 잰 값 ⑵ 고른 값(무엇과 무엇을 견줬는지). 어느 쪽도 아니면 그 수를 쓰지 말고 먼저 잰다.
    채팅에서 던지는 수도 같다. 기계는 `tools/audit_magic_numbers.py`(래칫이라 새 것만 막는다 —
    소급 면제 목록은 빚이고, 그 상수를 다시 만지면 근거를 적고 줄을 지운다).

17. **규칙을 쓸 때 「이걸 기계가 셀 수 있나」를 먼저 묻는다 — 판단형 판정선은 안 지켜진다.**
    - 셀 수 있는 형식 조건으로 바꾼다 — 「도구 호출 사이에 산문을 쓰지 않는다」·「사용자 발화 하나에
      보고 하나」. 요령은 「제목으로 쓸 수 있나」다(첫 줄이 `**` 나 `#`).
    - 재는 자를 만들고 거기서 멈추지 않는다 — 「이 수가 얼마를 넘으면 마감이 막히나」를 같이 정한다.
      정할 수 없으면 아직 판정에 못 쓴다는 뜻이고 그 사실을 적어 둔다.
    - 「진행 중계」가 그 형태로 닫혀 있다: 상한 사용자 발화 1회당 1줄 · 첫 줄이 굵은 제목인가(하한 60%).
      재는 자 `tools/audit_session_cost.py`, 막는 자 `tools/check_narration.py`.

18. **묻는 보고는 「제안」이 먼저 · 여러 건은 번호 목록부터** — 판정선은 사용자 층 [발화 생략] 절.
    세는 자 `tools/audit_session_conduct.py`. → 공용 「규칙/물어야-할-때.md」.

19. **요청한 만큼만 만든다** — 판정선은 사용자 층 [발화 생략] 절. → 공용 「규칙/요청한-만큼만.md」.

20. **매 세션 실리는 글(이 파일·`.claude/rules/**`)은 간결체** — 규칙은 사용자 층 [발화 생략] 절.
    래칫 `tools/audit_guide_size.py` — 자란 것은 사람이 판정하고, 정당하면 `--accept` 로 기준선을 옮긴다.
    → 공용 「규칙/실리는-글은-짧게.md」.

21. **자기 출력을 자기 증거로 쓰지 않는다** — 판정선은 사용자 층 [발화 생략] 절.
    기계 `tools/audit_stamps.py`·`audit_scope_claims.py`·`audit_gates.py`. → 공용 「규칙/증거의-정직.md」.

22. **중간에 쌓인 자료는 남은 범위를 좁히는 자다 — 다만 재서 줄인다** — 사용자 층 [발화 생략] 절.
    → 공용 「규칙/이미-가진-것으로-줄인다.md」.

23. **키워드·상시 트리거는 실리는 글에 있어야 발동한다 — grep-온디맨드로는 안 된다.** clear 준비 · 남은 일 ·
    단정 낮추기 · 찾았으면 고친다 · 확인하기 편하게 · 메모리·스킬 · 아티팩트 · 위반 원인 · 자격증명은 사용자 층이
    싣는다. 이 리포 고유의 둘만 여기 둔다.
    - **과목 진입 3단계** — ⑴ 진입 시 공용 폴더 `변경일지.md` 에 안 가져온 시스템 줄이 있으면(신고는
      `shared_sync_check` 훅) 스스로 판정해 가져오고 사후보고 한 줄 ⑵ 시킨 일을 한다 ⑶ 나가기 전,
      다른 과목에서 최근 생긴 공통 도구·규칙(`audit_convention_drift.py` 체크 목록 · 이 파일 최근
      변경분)이 이 과목에도 걸리는지 확인해 반영한다. 하나라도 빠뜨리면 "다른 과목은 고쳐졌는데 여긴
      안 됐다"가 과목을 바꿔가며 재발한다.
    - **진도 안 나간 것을 묻지 않는다** — 「이 절이 시험 범위인가」류는 **사용자도 아직 모른다.**
      범위 판정은 **강의계획서**(`data/<과목>/SUBJECT.md` 「범위」)로 내가 하고, 제안은 **그 진도에
      도달했을 때** 한다. 계획서에 절 단위 제외가 없으면 **포함으로 보고 채운다** — 수업에서
      빠지면 그때 뺀다. 답할 근거가 없는 물음은 빈손으로 묻는 것보다 나쁘다(2026-09-08 지적,
      **전 과목**). 물음을 인박스에 적을 때는 「☐ 언제」 칸을 채우고, 못 채우면 안 묻는다.
    → 원장 2026-09-02 · 2026-09-06.
## ★ 변경점 기준선 — 절차는 스킬 `review-baseline`

- **기준선을 옮기기 전 · `changeNote` 를 비우기 전 · 사용자가 [발화 생략]고 할 때 그 스킬을 연다.**
  언제 미나 · `reviewHold` · 챕터 한정 · 커밋 → 빌드 → 민다가 거기 있다.
- 뷰어·템플릿을 고쳤으면 `docs/viewer-change-log.txt` 에 한 줄(변경점 판정은 챕터 JSON 만 본다).
- 알림은 `review_baseline_due.py` 훅이 턴마다 낸다.
- **변경점은 사용자가 본 장에만** — 사용자가 화면에서 지적한 장은 그 자리에서 `set_review_seen.py --add`
  (선언 밖 장은 빌드가 기준선을 HEAD 로 유지한다).

## ★ close의 정의 — 회귀 테스트가 없으면 고친 게 아니다 (신설 2026-07-22)

- **데이터를 고치는 것은 그 인스턴스를 지우는 것이지 close 가 아니다.** 같은 결함이 내일 다시 들어오는
  것을 아무것도 막지 못한다.
- **close = `tools/test_checks.py` 에 ⑴ 결함을 재현하는 케이스와 ⑵ 고친 형태가 통과하는 케이스가 들어간
  상태.** 케이스에는 `열린 날짜 / 무엇이 새어나갔는지` 를 적는다. `tools/verify_workorder.py` 가 이
  테스트를 돌린다(항목 6) — 실패하면 완료를 주장할 수 없다.
- **세션을 닫기 전에는 `python tools/close_report.py`** — 빌드·회귀·`PENDING_FIG_FIXES` 잔량·strict 승격
  잔여·미커밋을 실제로 돌려 close/미완 표를 낸다. 기억으로 "통과"라고 쓰지 않는다. 도구가 못 보는
  영역(뷰어 JS·CSS 동작)은 따로 명시한다. → 원장 2026-07-22 「고친 게 아니라 인스턴스만 지웠다」.
- **새로 만드는 검사는 「전 과목 기본 error」다.** `is_strict_chapter` 의 기본값이 error 이고, 안 켤 과목은
  그 `index.json` 의 `strictWaivers` 에 **사유와 함께** 적는다(사유 없으면 면제 아님).
  옛 키 36개는 그대로 opt-in — `docs/검사-기본값-기준선.txt` 가 그 목록이고 **늘리지 않는다**.
  기준선 파일을 못 읽으면 엄한 쪽으로 넘어진다. 잠금 `test_new_checks_are_strict_everywhere_by_default`.
- **opt-in 승격도 close 를 막는다 — 「선언 안 함」과 「보류」를 가른다.** 과목별 승격
  (`index.json` 의 `strictChapters`)은 선언해야 켜지므로 안 적으면 경고 없이 꺼진 채 남는다.
  `close_report` 가 키마다 **선언됨 / 부분 / 미선언**으로 찍고, **게이트형인데 미선언이고 보류도 사유도
  없는 것만** close 를 막는다(전부 막으면 경보 피로로 검사기가 죽는다). 보류는 `pendingChapters`,
  안 켜는 이유는 `strictWaivers` 에 사유와 함께.
- **보고 규율:** "고쳤다"고 쓸 때 **데이터를 고친 것인지 검사를 고친 것인지** 구별해 적는다. 검사를
  고쳤으면 테스트 케이스 이름을, 데이터만 고쳤으면 **[발화 생략]** 이라고 명시한다.

### ★ 방지장치의 트리거는 「내가 반드시 하는 일」에 건다 (신설 2026-08-07)

「작업이 끝나는 순간」은 관측 가능한 사건이 아니라 *[발화 생략]* 형태의 규칙은 통째로 건너뛰어진다.
반드시 하는 일은 셋뿐이다 — **턴마다 보고를 쓴다 · 커밋한다 · 빌드를 돌린다.**

- 닫힌 예: 종료 타이머를 "루프 회차 끝"이 아니라 **Stop 훅**(턴마다)으로 옮겼다.
- 안 닫혔던 예: 「배치 끝에 기준선을 민다」는 배치의 끝이 관측 불가라 하루 세 번 지적받았고,
  「배치를 **닫는 마지막 단계**」로 옮겨 닫혔다.
- 판정: 방지장치를 설계했으면 *[발화 생략]* 를 묻는다.
  있으면 성실성에 기댄 것이다(규칙 7⑷ 위반).

### ★ 도구 등록부 — 정본은 `docs/도구-등록부.md` (안 싣는다)

**새 스크립트를 짜기 전에 그 파일을 먼저 본다.** 목록은 «부를 때 찾아보는 것»이라 매 세션 실을
자격이 없다(「올릴 자격」·실행 규율 20). 182줄이 실리고 있었고 2026-09-06 에 내렸다.

- 한 도구 = 한 줄(이름·용도·부르는 형태·**사람이 판정하는 자리**). 왜 열렸나·실측·잠금 테스트
  이름은 그 도구의 **독스트링**이 정본이다.
- 등록 감사 `python tools/audit_orphan_checks.py`(`close_report` 가 부른다) — 예외는
  `docs/orphan-checks-allow.txt` 에 사유와 함께. **등록 안 된 도구는 없는 것과 같다.**
- 「감사 ↔ 빌드 검사 대응표」(이건 이미 막히나?)도 그 파일에 있다.
## 사용자 지적 상태 연결 (2026-09-24)

- 질문에는 답만 한다. 결함 지적이면 **수정·설계 전에** 단발(동일 원인·경로가 다른 곳에 없음), 부류(같은 생성·표현 규칙의 반복), 시스템(접수·검증·종결 절차 또는 도구가 누락 허용) 중 하나로 판정한다. 대상 장 `chNN-review-inbox.md` 또는 공통 결함이면 `docs/받은것-인박스.md`에 `- ☐ ID · 관찰 | 분류=... | 범위=... | 원인=...`를 먼저 접수한다. 미확정 원인은 가설과 확인 방법을 적는다. 부류·시스템은 지적한 자리 외 유사 범위와 재발 경로를 조사한다. `python tools/feedback_intake.py <인박스> <ID> <분류> <관찰> <범위> <원인>`을 쓸 수 있다. 새 워크오더는 `Feedback-Inbox: <인박스경로>#ID`를 적고 실행한다. 외부 작업지시서도 같은 ID를 원본 인박스에 먼저 접수한다. 워크오더가 인박스를 대신하지 않는다.
- 장 JSON 수정 커밋은 해당 인박스의 열린 항목을 완료/부분 완료/미검증/무관 변경으로 재판정해 같은 커밋에 포함한다. 판단 없이 자동 ☑ 하지 않는다. `commit.py`가 새 접수의 분류·범위·원인과 파일 연결을 막지만 채팅 지적 자체의 누락이나 의미상 완료 여부는 사람이 현재 파일·화면으로 확인한다.

## 워크오더 — 절차는 스킬 `workorder`

- 큰 작업(챕터 재검·삽화 배치·구조 변경)은 `data/<과목>/<작업명>.workorder.md` 에 계획을 박는다.
  자잘한 단발 수정은 안 만든다.
- **완료라고 쓰기 전에 `python tools/verify_workorder.py <문서>` 가 exit 0.** 형식·게이트 항목·기준 커밋은 스킬에.

## 삽화(SVG) 표준 — 경로 규칙 `.claude/rules/figures.md`

- 장 JSON·삽화 생성기(`data/**/ch*.json` · `tools/gen_*.py` · `tools/svg_*.py` · `checks_svg.py`)를
  열면 실린다. 규격 정본은 `docs/삽화-규격.md`.
- 그 규칙이 안 실린 세션에서 `data/**/ch*.json` 을 쓰면 `guard_write` 가 막는다 — 먼저 그 장 파일을 `Read` 한다.
- **이관 방식(2026-09-11):** 특정 파일을 만질 때만 필요한 절은 `.claude/rules/*.md`(`paths:`),
  가끔 쓰는 절차는 `.claude/skills/`. 크기는 `audit_guide_size` 가 함께 잰다. 로드는 `instructions_log`
  기록으로 확인했다(2026-09-11 `path_glob_match`) — 규칙 파일을 **세션 중에** 만들면 그 세션엔 안 걸린다.

## 과목 작업 — 전 과목이 한 트리에 있다 (2026-09-06 구조 이전)

**브랜치·워크트리 분리는 없앴다.** `data/<과목>/`·`site/<과목>/` 이 전부 이 트리에 함께 있다.
옛 구조와 그것을 없앤 경위는 `docs/폐기된-규약.md` 가 정본이다(되돌리려는 사람이 먼저 읽을 것).

- **과목 = 폴더 하나**(`data/<과목>/`). 브랜치가 아니다. 한 세션이 여러 과목을 동시에 다룬다 —
  「한 세션 = 한 과목」·「과목 선언」·「남의 과목」은 전부 폐기된 개념이다.
- **다른 과목을 읽을 때 `git show` 를 쓰지 않는다.** 같은 트리에 있으므로 그냥 파일을 연다.
- **커밋은 여전히 경로를 명시한다.** `git add -A`·`git add .` 는 그대로 금지다 — 한 트리에 21과목이
  함께 있어 **사고 범위가 오히려 커졌다**(`guard_bash` 가 막는다).
- **그 과목의 사실은 `data/<과목>/SUBJECT.md`** 에 있다. 21개를 매 세션 싣지 않으므로 자동으로 안
  들어온다 — **그 과목을 만지기 전에 그 파일을 읽는다.**
- **`claude --worktree`(`-w`)를 이 리포에서 쓰지 않는다** — 워크트리를 더 만들면 합친 것이 도로 갈라진다.
- **미리보기 서버는 하나다** — `http://localhost:8800/<과목>/chNN.html`.
- **빌드는 한 장이 걸려도 끝까지 돈다.** 실패는 모아서 마지막에 한 번에 내고, 게이트(exit 1)는 그대로다.
## ★ 받은 지적은 피드백 원장에 먼저 적는다 (정본 이전 2026-08-16)

- **정본은 공용 폴더 `~/Documents/naru/기록/feedback-ledger.md`** 다. 리포 안에만 두면 프로젝트 사이 재발을
  볼 자리가 없다. 리포의 `docs/feedback-ledger.md` 는 머리에 「정본은 공용 폴더」를 박은 채 남아 있다.
- 지적을 받으면 **고치기 전에** 한 줄 적는다 — `부류(원칙)` · `범위(공통/과목별)` · `기계 방지`.
- **읽는 법은 통독이 아니라 조회다** — `python tools/feedback_lookup.py "<지적 요지>"`(원장 331KB).
- `기계` 칸이 빈 채로 항목을 닫지 않는다(「close의 정의」).
- **데이터 인스턴스 수정**은 원장이 아니라 `data/<과목>/chNN-review-inbox.md` 에 쌓는다.
- **승격된 것만 공통이 된다** — 지적을 워크오더에만 남기면 그 교훈은 그 과목에 갇힌다.

## 알려진 함정 (재발 금지)

- **`git diff --no-index` 로 리포 밖 파일과 비교하지 않는다**(2026-08-14 실측). 같은 파일 쌍을 두 번
  쟀는데 «31줄/56줄 다름» 과 «차이 없음» 이 번갈아 나왔고, 그 «차이 없음» 을 믿고 정정 보고까지 냈다 —
  `wc -cl` 로 재니 301줄 vs 276줄로 실제로 갈려 있었다. 원인 미확정(한글 경로·`core.autocrlf`·병렬 실행).
  **확정 못 한 것이 요점이다 — 이 조합은 조용히 틀린 답을 준다.**
  → 리포 밖 비교는 `wc -cl` 로 먼저 재고, 내용은 `diff --strip-trailing-cr -u` 나 양쪽 `Grep` 으로 본다.
  **재는 명령을 병렬로 여러 개 돌리지 않는다**(결과가 뒤섞인 정황이 같은 세션에 두 번).
- 데이터 함정(LaTeX·JSON·위첨자·아래첨자)은 `content.md`, 도구 함정(인코딩·「대상 아닌 과목」)은 `tools.md`,
  `hidden` 가드는 `viewer.md` 에 있다 — 그 파일을 열 때 실린다.

## 말투·진행 중계 — 이 리포에서 무엇이 막나

- 콘텐츠 존댓말 · 리포 문서·커밋 메시지 평어는 `C23`·`fix_honorific` 이 막는다. **채팅** 말투는 원작자 설정(`~할게`·`~했어`)이다 — 바꾸려면 `check_narration.py` 의 말투 판정도 함께 고친다.
  `tools/check_narration.py`(Stop 훅 매 턴 · 중계와 최종 보고 둘 다 · 「겠습니다」 예고 종결 포함)가 한 줄로 알린다.
- 재는 자 `tools/audit_session_cost.py` · 막는 자 `tools/check_narration.py`.
  **목표치로 쓰지 않는다** — 0 을 만들려다 물어야 할 것을 안 물으면 더 나쁘다.
- 하네스가 내는 「편집됨 파일 N개」 요약은 낭비가 아니다 — 줄일 것은 **내가 덧붙이는 해설**이다.

## ★ 공통 시스템은 `Documents\naru`(공용 폴더)에도 반영한다

- **자리:** `~/Documents/naru/` — `규칙/`·`훅/`·`도구/`·`기록/` + `README.md`.
  경로를 코드에 적을 때는 `shared_sync_check.SHARED` 를 임포트한다(환경변수를 존중하는 유일한 자리).
- **부르는 말은 「공용 폴더」다.** 프로젝트 사이 보고에서 «공용 폴더한테 보냈다»·«공용 폴더에서 받았다»로 쓴다.
- **트리거는 기계다** — `commit.py` → `sync_common.py` → `mirror_shared` 로 **커밋 하나에 리포 →
  공용**이 한 동작이다. 무엇을 옮길지는 `docs/shared-mirror.txt` 대장이 정하고, 공용의 기준은
  *«프로젝트 이름·도메인을 몰라도 성립하는가»* 다.
- **공용 쪽이 우리 모르게 바뀐 파일은 덮지 않고 신고한다.** **받아올지는 세션이 스스로 판정해
  가져오고 사후보고 한 줄을 남긴다**(2026-09-06 위임). `adopt_ledger.py` 가 자동 채택하지 않는 것은
  **기계**가 판정을 대신하지 말라는 뜻이지 세션이 미뤄도 된다는 뜻이 아니다.
  비교는 `diff --strip-trailing-cr -u <리포> <공용 폴더>` — 줄끝만 다른 파일이 많다.
- **레포 밖 쓰기 예외다**(규칙 9). 다른 경로로 넓히지 않는다.
- **과목·교재에 딸린 것은 넣지 않는다.**
- 잠금 `test_checks.py::test_shared_folder_mirror_is_machine_driven`.

## ★ 완료 선언 전에 산출물을 재생성했나

`data/**` 를 고친 뒤 완료를 선언하려면 그 전에 `tools/build_site.py`(또는 `close_report.py`)를 반드시
부른다 — **데이터만 보고 "끝났다"고 하지 않는다.** `/insights` 가 이 프로젝트의 최다 마찰로 잡은 것이
*[발화 생략]* 이다.

☐ 기계가 절반만 잡는다 — `audit_session_conduct.unbuilt_completion()` 이 Stop 훅에서 센다.
**못 잡는 것:** 세션 밖에서 미리 빌드해 둔 경우 · 그 빌드가 실제로 최신 데이터를 반영했는가.

## ★ 정본을 먼저 밝힌다

데이터를 고치기 전에 **어느 파일이 이번 작업의 정본인지**를 먼저 밝힌다. 확신이 안 서면 진행 전에 묻는다.
☐ 기계로 못 잡는다(선언이 없는 경우와 틀린 파일을 짚은 경우를 기계가 못 가른다).

## Claude 전용 메모

- 사용자 취향·진행 상태는 Claude 자동 메모리에 있다. **이 파일 내용을 메모리에 복제하지 말 것.**
- 감사 `tools/audit_content.py` · 워크오더 수용 `tools/verify_workorder.py` · 세션 닫기 전
  `tools/close_report.py`.
- 폐기된 규약(Codex 분업 · 과목=브랜치 모델 등)의 경위는 `docs/폐기된-규약.md`.

# Compact instructions

압축할 때 아래를 반드시 남긴다 — 계획·판정은 **파일**(인박스·워크오더)에 두는 것이 정본이고,
이 절은 그 파일을 가리키는 실이 끊기지 않게 하는 보조다.

- 지금 열려 있는 **워크오더·인박스 파일 경로**와 **몇 번 항목까지 닫았는지**
- **변경점 기준선** — `reviewHold` 선언 여부, 아직 안 민 챕터, 「커밋 → 빌드 → 민다」 중 어디인지
- 이번 배치에서 **고쳤다고 보고한 것**과 **아직 안 고친 것**의 구분(규칙 11)
- 사용자가 내린 **판정**과 그 말 그대로의 인용 — 판정은 요약하면 근거가 사라진다
